# T7a share image spike

Temporary browser-only spike for validating `html-to-image`, fixed-size Recharts, and MSU character image CORS behavior.

This directory is not wired into the production app.

