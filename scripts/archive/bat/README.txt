Archived .bat launchers (replaced by 3 scripts at repo root).

Use instead:
  run_local_dev.bat      UI development (sync + hot reload)
  run_local_preview.bat  Pre-push check (sync + production build)
  run_fetch.bat          API fetch (SQLite + rankings.json)

These files are kept for reference only.
