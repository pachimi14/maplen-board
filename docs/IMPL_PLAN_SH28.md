# IMPL_PLAN_SH28 — 装備候補からアイテムIDの表示を外す

前提: SH-27 完了・統括検収済。**ユーザー指示 2026-08-06**:
> アイテムIDは表示しないでください

## 0. 現状

装備の検索候補が `AbsoLab Ancient Bow #1592019 · ☆22まで` のように
**アイテムIDを表示**している。

## 1. 変更内容

**候補と選択後の表示から、アイテムID(`#1592019` 等)を消す。**

### 1-1 ★検索機能は維持する

**アイテムIDでの検索は残す**(設計 §7: 検索対象は「代表名 + 全 alias 名 + 全 itemId」)。
**表示を消すだけで、`1382265` と打てば引ける状態を保つ**こと。

- **(b) で「IDで検索できること」をテストで固定**する。
  **表示を消したついでに検索も壊した、を防ぐ**

### 1-2 残すもの

- 装備名
- **`☆22まで` のバッジ**(SH-9 で入れた `maxStar` の明示。**消さない**)

## 2. スコープ

**変更してよい**:
- `exp_ranking/web/src/sfhistory/components/EquipmentSelector.jsx`
- `exp_ranking/web/src/sfhistory/domain/equipmentSearch.js`(**表示用の整形のみ**。**マッチ条件は変えない**)
- `exp_ranking/web/src/i18n/locales/*.json`(**表示文言の変更が要る場合のみ・6ロケール同時**)
- 対応する `*.test.js`
- `docs/reports/SH28_HIDE_ITEM_ID.md`

**触らないもの**(1つでも触れたら停止):
- **検索のマッチ条件**(名前・alias 名・itemId のいずれでも引ける性質)
- **並び順**(SH-14 の `localeCompare` 昇順)
- **代表解決**(alias を選んだら代表 itemId で API を叩く。SH-9)
- `server/` 配下すべて / **4h テーブル**
- SH-7〜SH-27 の性質すべて
- `src/App.jsx` / `src/board/` / `src/pages/` / `src/components/` / `src/taskManager/`
- `package.json` / **VPS** / **元ツリー**

## 3. 受け入れ基準

- **(a)** 候補・選択後の表示に **`#<数字>` が出ない**
- **(b) ★ID検索は動く**: `1382265` と入力して Arcane Umbra Staff が候補に出る。**テストで固定**
- **(c)** alias 名(例 `AbsoLab Knight Gloves`)での検索が従来どおり動き、
  **選択後は代表 itemId(`1082637`)で API を叩く**
- **(d)** `☆22まで` のバッジは**残っている**
- **(e)** 並び順が `localeCompare` 昇順のまま
- **(f)** `npm run test` 全緑 / `npm run build` 成功 / **`server/` の差分ゼロ**
- **(g)** 6ロケールのキー数一致(文言を変えた場合)
- **(h)** SH-7〜SH-27 の性質維持

## 4. 停止条件

1. **表示を消すと ID 検索も消える構造**になっている(= マッチと表示が同じ文字列に依存している)
   → **止めて報告**。マッチ条件は変えない
2. §2 の「触らないもの」に触る必要が生じた / 新規依存が必要になった

## 5. コミット

- **ローカルコミット1本**。**`git push` は行わない**。**`git add -A` 禁止**。

## 6. 完了報告テンプレ

```
## SH-28 完了報告
- コミット: <hash>
- (a) ID が表示されないことの確認
- (b) ★ID検索が動くテスト
- (c) alias 検索 → 代表itemId で叩く
- (d) ☆上限バッジが残っている
- (e) 並び順
- (f) npm test / build / server 差分ゼロ
- (g) 6ロケールのキー数
- (h) SH-7〜SH-27 の性質維持
```
