# DECISION_LOG — maplen-board (lulumi-tools.com) 意思決定記録

最終更新: 2026-08-01 / 運用ルールは §0。**全セッションの標準コンテキストに含める正典文書**。

## 0. 運用ルール

- **追記のみ**(entry の書き換え禁止)。決定が覆った場合は新 entry を追加し、旧 entry の状態を「置換 → 新ID」に更新する
- 状態: ✅ 確定 / 🔶 暫定(実測・外部回答で見直し前提) / ⬜ 未決(判断待ち)
- 「決定」には必ず「理由」を付ける。理由のない決定は後続の実装判断を迷わせる
- 設計書(roadmap / improvement_plan / review)と矛盾が生じたら、**本書が優先**。設計書側を追随更新する

---

## 1. 決定(LULU)

| ID | 日付 | 状態 | 決定 | 理由 |
|----|------|------|------|------|
| LULU-001 | 2026-07-14 | ✅ | **北極星**: MapleStory N の Lv225+ 経験値ランキングを「あなたの毎朝の習慣」に変え、キャラ個別URL+OGで検索/SNSから人を連れてくる | 全ての設計判断の基準 |
| LULU-002 | 2026-07-14 | ✅ | 取得対象は **Lv225+ のまま拡大しない**(225未満はサブキャラとして切り捨て) | 戦略確定事項(roadmap 前提) |
| LULU-003 | 2026-07-14 | 置換 → LULU-040 | **バックエンドサーバーは持たない**。GitHub Pages 静的 + CI(GitHub Actions)で完結 | サーバーレス方針。運用コストと複雑度を持たない |
| LULU-004 | 2026-07-14 | ✅ | bot の**取得ロジック変更禁止**(`main.py` の fetch/リトライ/スキップ判定)。bot 側で触れるのは原則 `mvp_export.py`(エクスポート項目の追加)のみ | 取得パイプラインは競合優位が確立済み。壊さない |
| LULU-005 | 2026-07-14 | ✅ | キャラの**正準IDは `historyKey`(assetKey ベース)**。URL・ピン留め・比較のキーに **name を使わない** | 改名耐性。name をキーにすると後で全面書き直し |
| LULU-006 | 2026-07-14 | ✅ | 旧v1フォーマット(`data/rankings.json`、62MB)は**廃止方向**。新機能から参照しない。配信除去はクイックウィンとして独立実施可 | 廃止予定資産。触らず除去のみ |
| LULU-007 | 2026-07-14 | ✅ | UI文言を追加したら **6ロケール(ja/en/es/th/vi/zh-TW)全部に同時**にキー追加。**新規 npm 依存の追加は事前にユーザー確認** | 翻訳穴の根絶 / 依存の無断追加防止 |
| LULU-008 | 2026-07-14 | ✅ | localStorage キーは既存命名 **`maplen-board-*`** に合わせる | 既存の favorites/groups との整合 |
| LULU-009 | 2026-07-14 | 🔶 | **T12(ranking.db 保存先移行 + git 履歴掃除)は 2026-10-06 までの時限**。`.git` が db.gz 毎日コミットで肥大化(調査時点 1.6GB)。履歴書き換えは force push を伴うため実施タイミングはユーザー判断。**T12 スコープ(ユーザー確定 2026-07-14、LULU-015 の合流分を含む)**: ①DB保存先の移行 ②DB復旧設計の見直し ③v2+シャードを使った復旧への移行 ④ローカル bat の v2 対応 ⑤v1 生成停止 ⑥v1 配信停止。=62MB v1 の完全廃止は T12 で一括処理 | 3ヶ月以内の時限課題(roadmap)+ QW調査での合流(LULU-015) |
| LULU-010 | 2026-07-14 | ✅ | **作業プロトコル(ユーザー指定)**: ①各タスク着手前に「今回やることの要点」を解説し**着手可否を確認**してから進める ②着手後は**致命的バグ/プロジェクト根幹が崩れた場合のみ必ず報告**、それ以外の技術判断は**アドバイザー相談の上で統括裁量で作業完了まで進める** ③git **commit は統括裁量**で可 ④**push はユーザー判断を仰ぐ**(その前に必ずローカル動作チェック) ⑤**トークン節約**を常に意識 | ユーザー指定の回し方 |
| LULU-019 | 2026-07-14 | ✅ | **T2 仕様訂正: sort 変更も page=1(ユーザー裁定=選択肢B)**。当初 IMPL_PLAN_T2 §7 は「sort 以外の絞り込みで page=1」としたが、実装は T0 既存挙動(sort でも page リセット)を維持。ユーザーが B を選択し §7 を訂正: **「ランキング結果または並び順を変更する条件(sort・検索・各種フィルタ)が変わったら page=1。ページ送り操作のみ指定 page を維持」**。理由=既存挙動維持/T2 は URLステート対応でありUX変更ではない/並び順変更で同ページ番号は表示対象が大きく変わるため1ページ目が自然。数値・テキスト入力は `replace` で履歴を増やさないが値変更時は page=1。**現行実装が正(コード修正なし)** | ユーザー裁定 |
| LULU-018 | 2026-07-14 | ✅ | **T5 完了(本番検証済)**。PR #4 を `[web-only]` 付きで main へマージ(merge f8a4916)。Pages workflow(run 29305632670)success=**朝のデータ取得と別に Pages デプロイ正常終了**。本番検証全合格: `lulumi-tools.com/` 正常表示・英語 title・`/robots.txt`・`/sitemap.xml`・`/og.png`・favicon(svg/ico/apple-touch)すべて 200・favicon link 3種・**本番で言語切替→ title/description/html lang が更新**(ja 実機確認)。T0〜QW・T5 の SEO基礎パック完了。次=T2 | 統括(本番検証) |
| LULU-017 | 2026-07-14 | ✅ | **T5 検収=合格(push 前ゲート)**。実装担当のコミット3本(19ccab8/070e524/6dd48e4)を code-review+ローカル実機検証で検収。英語基準の静的SEO(index.html: title/description/canonical/robots/theme-color/OG(og:locale=en_US・alternate無し・hreflang無し)/Twitter summary_large_image/favicon svg+ico+apple-touch/lang=en/no-JSフォールバック英語化)+ 静的アセット(robots.txt・sitemap.xml=ルート1件lastmod無し・favicon.svg正・og.png)。実行時メタ更新=I18nContext effect に title/description 追加(英語フォールバック付き try/catch)。`npm run build` 成功・新規npm依存なし。dist に生成スクリプト漏れなし。og.png=1200×630(汎用"L"マーク+ブランド文字、**ゲーム/Nexon素材なし**)、apple=180×180。**6言語すべてで title/description/html lang の runtime 更新を実機確認**。**workflow/bot/vite/CNAME 不触**(差分空)。**push はユーザー判断待ち** | 統括検収 |
| LULU-016 | 2026-07-14 | ✅ | **QW 検収=合格(push 前ゲート)**。実装担当のコミット2本(df6288b/8f40059)を code-review+ローカル実機検証で検収。差分は計画どおり最小(取得候補を v2 のみに / エラー分岐を `loadErrorTitle`+`loadErrorHint` に / BoardHeader の生 loadError 行削除)。`npm run build` 成功。dev 実機: 通常時(v2)正常描画 / **v2 退避時=「Unable to load ranking data / Please try again later」の明確なエラー表示** / **v1(`data/rankings.json`)を一切 fetch しない**(v1 ファイル存在下でネットワークに v1 リクエスト0=フォールバック除去を実挙動で確認)/ 新規 Console Error なし(v2失敗時の catch ログは意図した error 経路、入れ子button警告は T0由来の既存)。i18n キーは6言語既存・en 翻訳描画確認。**push はユーザー判断待ち** | 統括検収 |
| LULU-015 | 2026-07-14 | ✅ | **QW 前提の訂正(調査+アドバイザーで判明。LULU-006「配信除去は独立クイックウィン」を部分置換)**。配信中の v1 `rankings.json`(約62MB、.gitignore済=未コミット、CIで生成)は**フロントのフォールバック以外に main.py の DB 復旧入力**でもある(`SNAPSHOT_IMPORT_FROM_PAGES` → `MVP_PAGES_RANKINGS_URL`)。ただし: ①その復旧経路は**フェイルソフト**(404→warning→0行、クラッシュしない)②git の db.gz 復元(第1層)+actions cache(第2層)が先に効くため通常は0行 ③v1復旧は**過去日 rank を最新日順で捏造する劣化コピー**(job_code空/rank_fluctuation=0)で資産価値低。**帯域**: 復旧は欠落判定前に**生62MBを全量DL**(urllib非圧縮)+meta hydrate で日約70MB≈月2GB強(Pages 100GB/月softに対し許容)。**フロントのフォールバックは防御価値ほぼゼロ(v1/v2はアトミック同時デプロイ)だが、v2リグレッション時に62MBを利用者に掴ませ隠す害が実在**(モバイルでパースがクラッシュ級)。**結論**: QW=**フロントのフォールバック除去+i18nエラー表示のみ**(安全・即効)。62MBの**配信停止**とPages容量削減は**復旧のv2+シャード移行(実現可能)とセットで T12 に合流**。**QW は T0(PR#1)マージ後に実施**(取得候補が App.jsx→board/useRankingBoard.js に移るため) | 調査+アドバイザー(助言は入力・裁定は統括) |
| LULU-014 | 2026-07-14 | ✅ | **T1 検収=合格**。実装担当のコミット2本を code-review+ローカル検証で検収。`mvp_export.py` は契約(LULU-013)に忠実: rankFluctuation パススルー/previousRank=latest−1暦日・null・遡らず/job・world 順位は truncation 前全人口・`(rank,historyKey)`安定ソート/null は例外なし・`_jobMissing`は出力前除去/rank一意ゆえ既存並び順・id 不変。テスト=既存28+新規12=**40 passed**、独立検証(実装の写しでない)・fixture駆動(PR-004)。実データ: previousRank null率0.79%(55/7004、停止条件非該当)・jobRank null 0件。**T0(web)混入を是正**: 当初 T1 が t0 ブランチに積まれていたため、`t1/export-rank-fields`(origin/main起点)へ cherry-pick 退避し t0 を4063f9cへ reset(PR #1 無汚染)。**push はユーザー判断待ち** | 統括検収 |
| LULU-013 | 2026-07-14 | ✅ | **T1 データ契約(承認済み設計+実データ検証で確定)**。mvp_export.py が character に追加する項目: (a)**`rankFluctuation`**=既存 API 値をそのまま出力(**新規 `rankChange` は作らない**。実DB突合で API値==`previousRank−rank` が 6949中6932=99.76%一致・符号同一、差は端の「比較不能時 API=0 / 我々=null」のみ。二重化回避のため既存名称を採用)。int、正=上昇/負=下降/0=変動なしor新規 (b)**`previousRank`**=latest_date−1暦日のスナップショットにおける当該identityのAPI rank、前日不在なら **null**(直近成功日へ遡らない) (c)**`jobRank`/`jobRankTotal`**=整形済み`job`完全一致グループ内のレベル順位と人数、job欠損なら**null**(エクスポートを落とさない) (d)**`worldRank`/`worldRankTotal`**=`worldId`一致グループ、空なら**null**(未知worldIdを仮グループ化しない)。**採番規則**: `rank`昇順の単純連番(rankは日次で一意=実証済、重複0/7004)、安定ソートキー`(rank, historyKey)`。**全人口で計算後に export_top_n を適用**。**`dataFormatVersion`=2 据え置き**(追加のみ・後方互換、シャード読込ゲート `===2` を壊さない)。派生順位の**正は mvp_export.py に一本化**(web/T3は消費のみ)。**UI消費契約**: 表示は `previousRank` を先に見る=`previousRank===null` なら `rankFluctuation===0` でも「変動なし」と解釈せず「比較不能(新規/前日欠測)」扱い。「新規」と「取得スキップ」は T1 では区別しない(必要時に別フィールド設計) | ユーザー承認+実データ検証。詳細 `docs/IMPL_PLAN_T1.md` |
| LULU-012 | 2026-07-14 | ✅ | **T0 検収=合格(push 前ゲート)**。実装担当のコミット4本(f06628c/c09c358/fb1ddb9/4063f9c)を code-review+ローカル実機検証で検収。App.jsx 963→65行、状態は `useRankingBoard`+`BoardContext` に集約(LULU-011 準拠、禁止事項の逸脱なし)。本番 v2 データを一時配置し dev で実挙動確認: `#/`描画/展開→URL遷移(encodeURIComponent)/新規URL・F5復元/戻る進む/Not Found(クラッシュ無・自動遷移無)/フィルタ往復保持/portal二重化なし/en・ja表示. `npm run build` 成功。**新規 Console Error 無**(観測された `<button>`入れ子 hydration 警告は未変更の TopGainHighlights/FavoriteStar 由来=原版から存在)。fetch は単一useEffect由来で回数不変(dev の二重は StrictMode、本番非該当)。**push はユーザー判断待ち** | 統括検収 |
| LULU-011 | 2026-07-14 | ✅ | **T0 確定仕様**: (a)状態管理=単一 `useRankingBoard()`+`BoardContext`。**T0では禁止**=Context分割/useReducer化/外部ストア/useMemo・useCallbackの最適化追加/状態構造の変更。現行ロジックを順序保存で移設し挙動完全一致 (b)**URLを詳細表示の唯一の正**とし `detailView` state を廃止(URLとの二重管理禁止)。ルートは `#/` と `#/character/:historyKey` のみ。グループ展開は routing せずローカル状態 (c)historyKey生成時 `encodeURIComponent`/解析時 `decodeURIComponent` (d)historyKey不在時=取得前Loading→取得後も無ければNot Found+一覧へ戻る導線、**自動で別キャラへ飛ばさない** (e)portalの構造・責務は不変、ターゲット不在でもクラッシュしないガードのみ維持 (f)`RankingListView` は画面合成のみ(データ取得・フィルタ・派生計算を戻さない) | ユーザー承認 + 追加要件。詳細は `docs/IMPL_PLAN_T0.md` |

---

## 5. 横断プロトコル(運用)

> 先行プロジェクト(Gear Simulator)で実証された既定値。合わない項目は削除でなく「不採用の決定」として記録する。

| ID | 日付 | 状態 | 決定 | 出典 |
|----|------|------|------|------|
| PR-001 | 2026-07-14 | ✅ | 1計画書=1縦切りテーマ=1PR=1関心事、コミットは単独 revert 可能な 4〜6個以内。**挙動不変ステップ先行、挙動変更は最後**。リファクタと機能追加を混ぜない | 先行実績 + roadmap §6 |
| PR-002 | 2026-07-14 | ✅ | 計画書必須項目: スコープ宣言 / 変わってよい・いけない箇所 / 数値の受け入れ基準 / **停止条件** / 検証コマンド / ロールバック / 完了報告テンプレ | 同上 |
| PR-003 | 2026-07-14 | ✅ | パラメータ調整は受け入れ基準内なら事後承認可。**アルゴリズム・データモデル・v2シャード形式・キャッシュキーの変更は事前承認必須**。逸脱発見時は選択肢を付けて報告 | 同上 |
| PR-004 | 2026-07-14 | ✅ | フィクスチャ駆動(外部 API 非依存のテスト)を共通要求。実キー・実秘匿情報は実装エージェントに渡さない。bot 変更時は `exp_ranking/bot` で pytest 通過 | 同上 |
| PR-005 | 2026-07-14 | ✅ | 実装中に設計と食い違ったら、コードでなく**先に設計書(と本書)を更新**してからタスクを切り直す | 同上 |
| PR-006 | 2026-07-14 | ✅ | 実装は implementer サブエージェント(orchestrator-kit)。統括は検収に回る(作者≠レビュアー) | 同上 |
| PR-007 | 2026-07-14 | ✅ | **挙動変更コミットは同一PRで baseline/golden 再生成を義務付ける**(運用している場合)。挙動不変の検証は「変更前後のコード直接比較」(git worktree)で行う | 同上 |
| PR-008 | 2026-07-14 | ✅ | **改行コードノイズをコミットに混入させない**: 一括 `git add -A` 禁止、意図したファイルのみ選択ステージし、各ファイル `git diff -w` で実質差分を確認してから add | 同上 |
| PR-009 | 2026-07-14 | ✅ | **整合テスト・golden への影響判定は、推定でなく照合対象の実物と突き合わせて行う**。①その量がどの照合フィールドに落ちるか ②その経路はフィルタ済みか全量か ③fixture に該当条件が存在するか、を確認してから影響件数を見積もる | 同上 |
| PR-012 | 2026-07-14 | ✅ | **標準開発フロー(ユーザーのローカル確認を必須ゲート化)**: 実装 → 統括(Claude)のコードレビュー・実機検証 → PR作成 → **★ユーザーがローカル環境で利用者目線の確認★(普段の操作/操作感/URL共有の直感性/フィルタ・検索・ページング/詳細往復/お気に入り・グループ/スマホ表示)** → 必要なら修正 → **ユーザーのマージ承認** → main マージ → 本番確認。**PR作成後はマージを前提に進めず、「ローカルで確認してください」で一度止める**。ユーザーが「ローカル確認OK」と伝えてから初めてマージ可否判断へ進む。統括の検収は「仕様どおり動くか」の確認として信頼されるが、公開前の利用者目線確認はユーザーが行う | ユーザー指定(2026-07-14) |
| PR-011 | 2026-07-14 | ✅ | **dev サーバ停止の作法(実装担当向け運用ルール)**: dev サーバ停止に `taskkill /F /IM node.exe` 等の**全 node プロセス一括終了を使わない**(ユーザーの他の dev サーバ・IDE ツール等を巻き添えにする)。停止は起動した特定プロセス/ポートのみを対象にする(preview ツールの stop、当該 PID 指定、該当ポートのプロセスのみ終了 等)。そもそも検証のための dev 起動は最小限にし、可能なら統括側の検証に委ねる | T5 実装時の巻き添え事故(LULU-017 watch-item)を受けたユーザー指定 |
| PR-010 | 2026-07-14 | 置換 → PR-013 | **アドバイザー運用**: 上位モデルの助言専任サブエージェント `advisor`(orchestrator-kit、read-only)。発動条件=①stuck-recovery 3周で不抜け ②全緑×体感矛盾 ③高リスク設計判断のセカンドオピニオン。助言は入力であって裁定ではない(検収=統括・決定=ユーザー)。追い質問は SendMessage。spawn が model 起因で失敗したら当該相談は `model: opus` 明示指定で続行、advisor.md の `model:` 行の恒久書き換えはユーザー確認必須 | 同上 |
| PR-013 | 2026-07-16 | ✅ | **アドバイザー発動条件を2クラス化(PR-010 を置換。他の運用条項=read-only/SendMessage/model フォールバックは PR-010 のまま有効)**: **①行き詰まりクラス**(従来どおり: stuck-recovery 3周不抜け / 全緑×体感矛盾 / 高リスク設計判断)。**②前提レビュークラス(新設)**: **回収コストが高い作業の開始前**に発動=実行30分超 or 100kトークン超が見込まれる、またはやり直しが全損になる作業(履歴書き換え・データ再生成・長時間テスト)。依頼内容は実装相談でなく**前提の監査のみ**(「この作業の前提A,B,C のうち崩れていそうなものと、各前提の最安検証法」)。あわせて**計画書必須項目に「前提リスト+最安検証(パイロットラン)を本番実行前に実施」を追加**(PR-002 の拡張)。契機=前提誤りのまま 300kトークン/1時間のテストを実行し全量やり直しになった事故(2026-07 gear sim 側)。3回スタック条件は「見えるスタック」しか拾えず「自信を持って間違う」クラスを検出できないため | ユーザー指摘(2026-07-16)+統括裁定 |

---

| LULU-026 | 2026-07-14 | ✅ | **主キャラ自動昇格を確定仕様に(ユーザー確定、LULU-025 watch-item を解消)**: ピンが1体以上あれば `primaryHistoryKey` は必ず存在(最初のピンを主に)/ 主解除時は残り `pinnedHistoryKeys` の先頭を次の主に / ピン0体のときのみ null /「ピン済みだが主なし」状態は作らない。現行実装のままで確定 | ユーザー確定 |
| LULU-027 | 2026-07-14 | ✅ | **T4a 完了(main マージ・確認済)**。PR #10 を `[web-only]` で main へマージ(merge 6e5cf67)。main で `npm run test` **107件全緑**・`npm run build` 成功。**ProfileProvider は引き続き未配線**(App/main が未 import)=**bundle 未混入=既存UI・favorites/groups・旧localStorageキー 不変**。本番 200・deploy は bundle 不変の no-op。bot・朝のデータ取得 workflow 不変([web-only])。次=T4b(既存ホーム上に成績サマリー表示、Provider を初配線) | 統括(マージ後確認) |
| LULU-025 | 2026-07-14 | ✅ | **T4a 検収=合格(push 前ゲート)**。実装担当2コミット(241abd8/87bf125)を code-review で検収。`src/profile/`(profile.js=純粋+storageアダプター、ProfileContext.jsx=Provider)。契約適合を確認: 3層分離、目標正規化(整数225-275+実在暦日・両方必須)、readProfile の status 5区別(ok/missing/corrupt/unsupportedVersion/storageError=classifyPayload に単一集約)、共通結果形式`{ok,code,profile}`、**save-then-commit**(ok時のみ setState・失敗で既存不削除)、**unsupportedVersion 非破壊**(mutate/write を通さず拒否)、storageイベント(PROFILE_KEYのみ・旧favorites/groups無視・null→空・冪等・cleanup)、不変条件(MAX_PINS=3・primary補正・limitReached自動削除なし・historyKey欠損invalidKey・主削除→次主先頭・summary非依存・目標非自動削除)。**107テスト全緑**(既存48+新59)・`npm run build` 成功・**Provider 未配線=bundle未混入=UI不変**・**favorites/groups(`msu_exp_ranking_*`)・本番vite.config・bot・workflow・package.json 不変・新規依存なし**(vitest.config は test include 拡張のみ)。watch-item: primary は「pinがあれば必ず非null(先頭自動昇格)」=仕様の literal reading・製品的に妥当だが T4b で意図確認推奨。**push はユーザー判断待ち** | 統括検収 |
| LULU-024 | 2026-07-14 | ✅ | **T4 プロファイル層の方針(ユーザー確定)**。(a)**既存お気に入り/グループを T4 で吸収しない**。`msu_exp_ranking_favorites`・既存グループ関連キー・保存形式・お気に入りUI は**一切変更しない**(name依存のまま維持)。(b)**新規プロファイル層は `maplen-board-profile-v1`**(LULU-008 の新規命名規約に沿う。旧キーは互換で維持し新機能を旧命名へ拡大しない。`v1` で将来のスキーマ移行余地)。(c)ピンは**小リスト**(主キャラ+サブ、初期上限3体、`primaryHistoryKey` を明示。先頭=主の暗黙仕様にしない)。identifier=**historyKey**(改名耐性)。(d)T4a(保存層)と T4b(表示層)は**別PR**。T4a マージ時点で利用者向け画面は不変。サマリーは既存ホーム`#/`上部(TOP3の上)に載せ**新規画面/ルートは作らない**。目標設定UIはカード内、計算は既存rankingUtils/T3 再利用(プランナーと二重実装しない)。**localStorage の命名は台帳を書き換えず事実を記録**: 既存 favorites/groups=`msu_exp_ranking_*` 維持 / 新規=`maplen-board-profile-v1` / 将来統合時に一度だけ安全な移行を設計 | ユーザー指定 |
| LULU-023 | 2026-07-14 | ✅ | **T3 完了(main マージ・確認済)**。PR #9 を `[web-only]` で main へマージ(merge d1bd90e)。main で `npm run test` **48件全緑**・`npm run build` 成功。Pages workflow success。**stats は未 import=ツリーシェイクで本番 bundle 非混入=既存ランキング画面 挙動不変**(grep で確認)。本番 200・title 正常。bot・朝のデータ取得 workflow は不変([web-only] でデータ取得スキップ)。T1〜T3(データ契約→URLステート→派生統計)完了。次=T4(既存ホーム上に自キャラ成績サマリー表示) | 統括(マージ後確認) |
| LULU-022 | 2026-07-14 | ✅ | **T3 検収=合格(push 前ゲート)**。実装担当の4コミット(588b3f7/37036a5/1cd44c8/cea43b0)を code-review で検収。`src/stats/`(A `calculateTopPercent`/B `computePassedAndOvertaken`/C streaks/D `computeDailyGainSelfRank`/E `computeGoalProgress`/F `calculateAverageDailyGain`)を契約どおり実装。**正の一本化を確認**: bot フィールド(rank/previousRank/jobRank/worldRank/dailyRank)は読むだけ・再計算なし、`rankingUtils.js` **不変**(import 再利用のみ)、履歴正規化は `normalizeHistoryPoints`(streaks.js)に単一集約。B=交差式どおり・historyKey欠損除外・入力順非依存、C=current は最新確定日基準・暦日連続・欠測終了、D=競技順位、E=averageDailyGain 外部注入・status順序(achieved→insufficientData→behind/ahead/onTrack)、F=0増加日含む/欠測分母外。**vitest は devDep のみ**(本番 deps 不変)・**48 tests 全緑**・`npm run build` 成功・scope は stats/+package.json+vitest.config のみ(bot/workflow/UI 不変)。**push はユーザー判断待ち** | 統括検収 |
| LULU-020 | 2026-07-14 | ✅ | **ローカル dev の v2 同期対応(T12 の一部を前倒し)**。`sync_rankings_from_pages.py` は v1 `rankings.json` を取得しローカル v2 を削除していた=QW 以降(アプリは v2 のみ・v1 フォールバック無し)は通常の `run_local_dev.bat` がエラー画面になっていた。修正: sync を **v2 summary + 全シャード取得**に作り替え、v1 取得と v2 削除を撤去。`run_local_dev.bat`/`run_local_preview.bat` の `--no-sync` 存在チェックを v2 summary に変更。**dev ツールのみ(main.py・本番パイプライン・web src・workflow に非影響)**。本番の v1 生成・配信自体は T12 まで維持(LULU-015 の②〜⑥は T12 に残る) | ユーザー指定(2026-07-14)。詳細 `docs/IMPL_PLAN_local-dev-v2-sync.md` |

| LULU-021 | 2026-07-14 | ✅ | **ブランドアクセント色をオレンジ→緑に変更(競合との被り回避)**。T5 で生成した favicon/OG のアクセントが amber `#fbbf24`(黒地にオレンジ)で競合サイトと色被り。サイトの実配色は**紺 `#0f172a` + 緑**(増加量表示の emerald-400 `#34d399`、実測 web src で最多使用)。→ アクセントを **`#34d399`(緑)** に統一(背景 `#0f172a` は維持=案A)。対象: `favicon.svg`(fill)/ `generate-seo-assets.py`(ACCENT_COLOR)→ 再生成した `favicon.ico`/`apple-touch-icon.png`/`og.png`。theme-color `#0f172a` は不変。ユーザーが「黒」と評したのはタブの favicon | ユーザー指定(2026-07-14)。ブランチ `chore/brand-color-green`(main起点・独立) |

| LULU-028 | 2026-07-14 | ✅ | **T4b 実装 + P0 修正 + 統括実機検証=合格(push 前ゲート)**。実装担当が T4b(ホーム上の自キャラサマリー)を5コミットで実装。統括の実機検証で **P0 バグ検出**: `MyCharacterSummary` のシャード取得 effect が deps に `historyFetch`/`characters` を含み、`loading` の setState が effect 再実行→前 run の cleanup が `cancelled=true`→取得結果を破棄=**履歴が事前ロードされていない主/サブキャラで「もっと見る」が loading のまま stuck**(rank1 以外の主キャラ=実利用で常発)。差し戻し→修正コミット `2a373dd`(deps を `[expanded, displayedHistoryKey, ensureHistories, retryTick]` に削減・retryTick・`classifyHistoryAvailability` に history 実在チェック・`shouldStartHistoryFetch` 純関数+テスト、3ファイルのみ)。**再検証(実機)合格**: galbi(非事前ロード)で show-more→履歴ロード成功(連續43天・歷代22位)/多重fetchなし(dev の2回は StrictMode)/ 未登録CTA(コンパクト)/ 詳細「加入我的角色」→サマリー切替・excess 表示(順位変動▲1・上位%・**抜いた=等級排名超越+「日増量排名ではない」補足**)/ 目標モーダル(a11y・保存時のみ閉じる・localStorage永続・T3 E で「領先10天」表示)/「最新成績」+対象日(最新日≠端末今日)。**135テスト緑・build成功・stats/profile/favorites/groups/bot/workflow 不変**。※スクショはブラウザペイン制約で取得不可=**視覚/操作感/モバイル/全6言語の最終確認はユーザーのローカル検証**(新フロー PR-012)。**push はユーザー判断待ち** | 統括検収(実機・差し戻し1回) |

| LULU-029 | 2026-07-14 | ✅ | **T4b 一覧レイアウト再構成(§21)実装 + 統括実機検証=合格(push 前ゲート)**。実装担当4コミット(77f8475/a312d9c/995990b/ce36e3f)を code-review+実機検証。**実機確認(1280px)**: 一覧「リンクをコピー」廃止・**詳細ページに「複製連結」移設**(現URLコピー)・**行クリックで1回で `#/character/:key` へ遷移**(サイドバー非展開)・**右列=マイキャラ常設**(geometry: myChar.left841 > table.right831)・**「群組比較」トグルで GroupPanel を一覧の上に開閉**(URL不変=ローカルstateのみ)。**実機(375px)**: **TOP3(y207)< マイキャラ(y1064)**=マイキャラは TOP3 の下・**横スクロールなし**。135テスト緑・build成功・**7ファイルのみ変更**(stats/profile/favorites/groups/i18n/bot/workflow/vite.config 不変・新規依存なし・group.title 6言語再利用)。GroupPanel B案(character anchor=主マイキャラ→最上位フォールバック+groupAnchorId override)。selectedId は詳細ルート用に board 維持・一覧用途のみ削除。watch: GroupPanel を RankingTable の groupPanel prop で描画(絞り込み下・行上=文言満たす)・TopGainHighlights のシグネチャ変更。※スクショはペイン制約で不可=最終の視覚/操作感/6言語/save失敗/高速切替はユーザーのローカル確認。**push はユーザー判断待ち** | 統括検収(実機) |

| LULU-030 | 2026-07-14 | ✅ | **T4b 第2ラウンド(§22)実装 + 統括実機検証=合格(push 前ゲート)**。ローカル確認後の追加修正=決定A(CharacterDetail 不変・カード側でヘッダー再現)/決定B(目標編集はカードに残し3構成へ)/決定C(グループ比較を `#/group` 専用全画面ルート)。実装担当5コミット(75e0509/141a953/039a807/92c2d71/76b6c00)。**実機検証(1280px)合格**: マイキャラカード=詳細準拠(画像・名前・職業/サーバー・Lv/EXP%・順位・cyan配色)/ **Main/Sub 表記**(スイッチャー・バッジ・ピン)/ **順位省略なし全表示**(職業内#8/822 前1.0%・サーバー内#7/2467 前0.3%=旧3列 truncate の見切れ解消)/ **もっと見る廃止・履歴は表示時遅延取得**(非事前ロード galbi で連続43天等表示=**P0非再発**)/ **目標3構成**(①Lv260・2026/9/1+編集/削除 ②今日+455.3B/必要+759.4B/達成率60%=今日÷必要 ③到達2026/10/5・落後34天、前提明記・0/欠測は推定不能)/ 詳細導線(名前・詳細を見る→`#/character/…?sort=weekly&page=2` クエリ保持)。**`#/group` 全画面**(TOP3/一覧/マイキャラ非表示・返回列表→クエリ復元・改名/削除/メンバー削除/お気に入り追加/チャートサイズ/7・30・カスタム期間)+ **比較表**(角色/職業/伺服器/Daily/Weekly/Monthly/期間 合計⇔平均、`buildGroupGainSeries` の series 再利用=チャートと同一ソース・既存グループ localStorage 旧形式`msu_exp_ranking_groups`のまま表示=互換維持)。**375px**: TOP3<マイキャラ<操作の積み上げ順・横あふれ0・比較表は overflow-x-auto で横スクロール。純粋関数 `buildGoalDisplayModel`(computeGoalProgress 1回呼び・pace=今日の増加量注入)/ `buildGroupComparisonRows` を追加(UI 重複計算なし)。**161テスト緑・build成功・6言語パリティOK(280キー)・CharacterDetail.jsx/useRankingBoard.js は diff ゼロ=決定A厳守**。**検収中に別件検出→差し戻し1回**: TOP3 `PeriodTop3` の行が実 `<button>` でその中に★`<button>` がネスト(§21/PR#11 由来のハイドレーション/DOM検証エラー)→ 修正コミット `94f6dca`(行を `<div role="button" tabIndex onClick onKeyDown>` 化、★は既存 stopPropagation 維持、TopGainHighlights のみ)。**再検証: live DOM で行=div[role=button]・ネストボタン0・TOP3クリック遷移維持を確認**(※実機コンソール読取ツールは再読込で消えない履歴バッファを返すため、判定は live DOM の outerHTML と新サーバの「No server errors」で確証)。※最終の視覚/6言語/操作感スクショはユーザーのローカル確認(ブラウザペインの screenshot はタイムアウトで取得不可)。**push・PR更新・main マージはユーザー最終確認待ち** | 統括検収(実機・差し戻し1回)。詳細 `docs/IMPL_PLAN_T4B.md` §22 |
| LULU-031 | 2026-07-14 | ✅ | **§22 グループ側=「表示移動のみ」へ縮小(ユーザー確定仕様)+ 是正実装 + 統括実機検証=合格**。ユーザー確定: 今回のグループ側の目的は「右側にあったグループ表示を `#/group` 全画面へ移す」ことのみで、**GroupPanel/CharacterGroupTools/計算/データ/localStorage/操作/グループ仕様は一切変更しない**。→ 第2ラウンド(LULU-030)のグループ実装が過剰(比較表=新挙動追加・GroupPanel 削除・CharacterGroupTools 改変・独自アンカー)だったため撤回・是正。実装担当コミット `f081092`: **GroupPanel.jsx / CharacterGroupTools.jsx を ce36e3f に復元(= §22前と完全一致、`git diff ce36e3f..HEAD` 空で確認)**・比較表3ファイル(`GroupComparisonTable.jsx`/`groupComparison.js`/`.test.js`)削除・`group.compareTable.*` を6ロケール削除・`GroupCompareView.jsx` を**既存 GroupPanel をそのまま全画面描画する薄いページ**に書換(§21/LULU-029 のアンカー解決 override→primary→`rankingPool[0]??characters[0]`→null を無変更で移設・戻りは GroupPanel 既存 `onCollapse`→`navigateToList()`)。**実機検証合格**: `#/group?…` で既存グループパネルが全画面表示(群組選択/改名/削除/メンバー/追加/チャート/7・30・カスタム期間)・**比較表なし(hasTable=false)**・既存グループ(旧形式 localStorage)そのまま表示・「返回簡易檢視」→ `#/?sort=weekly&page=2`(クエリ復元・一覧+マイキャラ復帰)。**152テスト緑・build成功・6言語パリティOK(276キー)**。グループ側の net 変更は `GroupCompareView.jsx`(69行)新規のみ。マイキャラ §22.1-3・ネストボタン修正(94f6dca)は無変更。**push・PR更新・main マージはユーザー最終確認待ち** | 統括検収(実機・仕様縮小1回) |

| LULU-032 | 2026-07-15 | ✅ | **§22.11 第3ラウンド(カードUI微調整+プランナー到達日1日ずれ修正)+ 統括検収=合格**。ユーザーのローカル確認後の要望。実装担当3コミット(`fe324f1` プランナー / `121d214` カードUI①-⑥ / `501b4ba` 確定契約テスト)。**カード**: ①増加量を詳細と同じ緑 `text-emerald-400`(実機 oklch 確認)②「詳細を見る」をヘッダー右上へ(vdTop=27px・スクロール不要)③「最新の成績」タイトル・データ日時を削除(右上に取得日時が常時あるため重複回避)④自己歴代順位から「(比較可能N日中)」削除・1行化⑤連続順位を tier 階層化=純粋関数 `pickBestRankStreak(history,[5,10,50,100,500])`(**`current` 使用・`current>=2` の最厳 tier 1件・該当なし null**、ユーザー確定4例+境界を独立テストで固定)⑥目標「削除」ボタン撤去(削除は GoalModal 内に既存=重複)。**プランナー**: 到達予定日オフバイワン修正=純粋 `arrivalDatePartsForDailyRuns(days)`(today+(days−1)、`days≥1` ガード)を `rankingUtils.js` に追加し `CharacterPlannerTools.jsx:184` を差替。**必要日数 `estimateDaysToLevelWithGain` は不変**。境界テスト固定(1日→今日/2日→翌日/11日→10日後=7/24)。実機: JST today 2026-07-15 で「Lv251・約1天・2026/7/15」=1日必要→今日(旧実装なら 7/16)。**170テスト緑・build成功・6言語パリティOK(276・i18n無変更)**。**決定A+グループ不変を厳格確認**: `git diff ce36e3f..HEAD -- CharacterDetail.jsx GroupPanel.jsx CharacterGroupTools.jsx` = 空。プランナー修正は別ファイル `CharacterPlannerTools.jsx`(決定A対象外)。**push・PR更新・main マージはユーザー最終確認待ち** | 統括検収(実機) |

| LULU-033 | 2026-07-15 | ✅ | **§22.12 第4ラウンド(導線ボタン視認性)+ 統括検収=合格**。実装担当1コミット `60a58cd`。**#1** マイキャラカード「詳細を見る」を text リンク調→**枠付きボタン**(`border-sky-500/50 bg-sky-500/10 px-2.5 py-1 text-sm` + ArrowRight、実機 101x32・14.9px)。位置(ヘッダー右上)・遷移(navigateToCharacter・クエリ保持)不変。**#2** 詳細画面(`#/character`)の戻る導線: `CharacterDetailView` から `CharacterDetail` への `onCollapse` を渡さず、ヘッダー内の小「簡易表示に戻る」(`CharacterDetail.jsx:644` の `isExpanded && onCollapse` ゲート)を**非表示**化。代わりに詳細最上部に**大型「一覧に戻る」ボタン**(既存 i18n `route.backToList`・ArrowLeft・`variant=outline`・実機 top155・87x56)を新設、`onClick={collapseDetail}`。実機: 「返回列表」クリック→ `#/?sort=monthly&page=3`(クエリ保持)・小「簡易表示に戻る」消失(smallCollapsePresent=false)。**決定A厳守**: `CharacterDetail.jsx`(+ GroupPanel/CharacterGroupTools)は `git diff ce36e3f..HEAD` = 空(無変更)。i18n 変更なし(既存キー再利用)。**170テスト緑・build成功**。`#/group` の戻るボタンは今回対象外(無変更)。**push・PR更新・main マージはユーザー最終確認待ち** | 統括検収(実機) |

| LULU-034 | 2026-07-15 | ✅ | **§22.13 第5ラウンド(サーバー名リンクのバグ+戻るボタン視認性+カード内登録)+ 統括検収=合格**。ユーザー確定: ①一覧＋詳細両方、③実装する。実装担当3コミット(`5c51a88` #1 / `0bcbb44` #2 / `81ed5ce` #3)。**#1**: 名前だけでなくサーバー名(worldId)も外部ナビゲーターへ飛ぶバグ。`RankingTable.jsx:176` と `CharacterDetail.jsx:674` の worldId `NavigatorLink` をプレーン span に(名前リンクは維持)。**決定A はこの1点のみ緩和**(`git diff` は worldId をリンクから外す3行のみ・他ゼロ)。実機: 一覧の行 `<a>` は名前のみ・サーバー「Fang」非リンク / 詳細も detailServerIsLink=false・detailNameIsLink=true。**#2**: 詳細の「一覧に戻る」を outline(近黒背景に埋没)→ Button 既定塗り variant(bg-blue-600・白・102x59)で視認性向上。CharacterDetail 不変。**#3**: マイキャラカード内で検索→直接ピン(往復解消)。`CharacterSearchPicker` に任意 `placeholder` prop 追加(後方互換)、`MyCharacterEmptyCta` を検索登録UIに、`MyCharacterSwitcher` に「＋サブキャラ追加」(ピン≥1 かつ <MAX_PINS=3)、`MyCharacterSummary` に `registerMode`/`handleRegisterSelect`(既存 `useProfile().pin` 再利用・alreadyPinned=成功扱い)。実機フル検証: 空状態で galbi 検索→選択→pin→カードが Main 表示・「＋サブ追加」出現 / サブ追加→登録モード(キャンセルあり)→Benjapol 検索→pin→pins=[galbi,Benjapol]・チップ「galbiMain/BenjapolSub」・登録モード自動クローズ。**280テスト→170緑・build成功・6言語パリティOK(280=register4キー追加)**。グループ2ファイル(GroupPanel/CharacterGroupTools)は `git diff ce36e3f..HEAD` 空=無変更。watch: `RankingListView` の `focusSearch`/`onFocusSearch` 経路は #3 で EmptyCta が使わなくなり実質デッド(RankingTable の検索 ref は独立機能・実害なし。将来クリーンアップ候補)。#2 の青(blue-600=DS 既定 primary)はブランド(紺+緑 LULU-021)と厳密には非一致=ユーザーに緑への変更可を提示。**push・PR更新・main マージはユーザー最終確認待ち** | 統括検収(実機・フル操作確認) |

| LULU-035 | 2026-07-15 | ✅ | **T4b(§21〜§22)ブランチ push + PR #11 本文更新(ユーザー承認済み・mainマージは未実施)**。push 前検証で **`exp_ranking/bot/data/ranking.db.gz`(bot DB・T4b対象外)の混入を検出**: ブランチ2番目のコミット `224f12e` が web と無関係に DB のみ変更(main版 `b5e4bfe` ≠ ブランチ版 `cbb14df`、マージで main データ破壊リスク)。ユーザー確定方針で **main版 DB を復元する専用コミット `b694dbb` を追加**(履歴書き換え・rebase・force-push なし=224f12e は履歴に残すが net 差分を無効化)。→ `git diff main...HEAD` から ranking.db.gz 消失・`exp_ranking/bot/**`・`.github/**` の差分空・**web(`src/**`・i18n・テスト・vitest.config)33ファイルのみ**(11 A / 22 M / 0 D、GroupPanel/CharacterGroupTools は net 無変更で差分に出ない)。**170テスト緑・build成功・6言語パリティOK(280)**。通常 push(fast-forward `2a373dd..b694dbb`)。PR #11 本文を §21〜§22 最終仕様へ更新(一覧再構成/マイキャラカード/目標3構成/レベルプランナー到達日/グループは表示移動のみ/不具合修正/検証結果)。**PR #11 = MERGEABLE / CLEAN**(https://github.com/pachimi14/maplen-board/pull/11)。**main マージはユーザーが最終差分確認後に別途指示**。ここで停止 | ユーザー承認(push・PR更新のみ)。統括検収完了 |

| LULU-036 | 2026-07-15 | ✅ | **PR #11 を main へ [web-only] マージ + Pages デプロイ完了 + 本番実機検証=合格(ユーザー承認済み)**。マージ規約: workflow `maplen-board-pages.yml` は**マージコミットに `[web-only]` を含めると `SKIP_MSU_RANKING_FETCH=true`**(API再取得なし・データ不変)かつ deploy=true(web を build して Pages 配信)。→ `gh pr merge 11 --merge` で `[web-only]` マージコミット `a23ddb7` を作成(PR#9/#10 と同規約)。**Pages workflow run `29351085302` = build/commit-db/deploy すべて success**(警告は Node20 非推奨・キャッシュ保存のみ=非ブロッキング)。マージ後 `a6d7b77 chore: update ranking.db [skip ci]` は**通常のパイプライン DB 保全**(毎デプロイの標準挙動・`[skip ci]` で再起動なし)。**データ不変の裏付け**: CI は [web-only](fetch なし)で b5e4bfe から実行し cbb14df を生成=同一データの再 gzip(スナップショット増減なし)。**本番(lulumi-tools.com)実機検証**: ①Pagesデプロイ完了 ②トップ正常(TOP3・データ「14 Jul 2026」=7/13 反映) ③行クリック→`#/character/…`直接遷移 ④デスクトップ右列マイキャラ(myCharLeft841≒tableRight842) ⑤カード内検索で galbi 登録→Main/増加量/目標/詳細を見る表示(§22.13 最新ビルド配信確認) ⑥詳細のリンクコピー有 ⑦プランナー「Lv.255 約5日 2026/7/19」=today(7/15)+(5-1)=7/19(オフバイワン修正稼働) ⑧`#/group` 全画面(TOP3/一覧なし)・戻りで `#/?sort=weekly&page=2` 復元 ⑨サーバー名は非リンク(名前のみリンク)。**bot・朝のデータ更新に影響なし**(`.github/**` 差分ゼロ=schedule 不変・[web-only] で fetch せず・[skip ci] で再起動なし)。**オープンPR: なし**(PR#11 MERGED)。**作業ツリー: tracked 変更なし・クリーン**(未追跡は統括ドキュメントのみ)。ブランチ `t4b/my-character-summary` は origin と同期・未削除。**T4b(§21〜§22)完了・本番稼働** | ユーザー承認(マージ)。本番検証合格 |

| LULU-037 | 2026-07-15 | ✅ | **マージ後是正2件(§22.14 プランナー到達日=snapshot基準 / §22.15 画像拡大)を新ブランチ `fix/planner-arrival-snapshot-anchored`(origin/main 起点)に実装 + 統括検収=合格。push/PR/マージは未実施(ユーザー指示待ち)**。**§22.14(バグ)**: §22.11 B の私の修正 `today+(N-1)` は **stale データ(today≠snapshot+1)で+1ずれる**不完全修正だった(本番: データ7/13末・実時刻7/15 で N=5 が7/19表示、正=7/18)。→ 誤 `arrivalDatePartsForDailyRuns` を削除し純粋関数 **`arrivalDatePartsFromSnapshot(snapshot,N)= datePartsFromIsoDate(addDaysToIsoDate(snapshot,N))`**(=**最新スナップショット日+N**、TZ非依存)へ置換(250/275マイルストーンと同一基準に統一。目標カード③は元から snapshot 基準で正=不変)。コミット `f249a05`。テスト: N=1→7/14 / N=5→7/18(reference=7/15 指定で snapshot 基準を証明)/ N=11→7/24 / 月跨ぎ / N<1・非有限→null / snapshot欠落→fallback(today+N)。**実機 stale 検証**: JST7/15・データ7/13 で galbi「Lv.251 約1日 2026/7/14」= snapshot+1(旧なら7/15)。**§22.15**: マイキャラカード画像 `w-20/md:w-24`→`w-28/md:w-32`(`MyCharacterCard.jsx`)、詳細展開画像 `w-24/md:w-28`→`w-28/md:w-32`(`CharacterDetail.jsx`、**決定Aを画像サイズ1行のみ緩和**)。コミット `a036fa5`。実機: カード・詳細とも画像 ~128-136px に拡大・横あふれ0。**172テスト緑・build成功**。`git diff origin/main -- CharacterDetail.jsx` は画像1行のみ・GroupPanel/CharacterGroupTools 空。**push・PR・main マージはユーザー指示待ち** | 統括検収(実機・stale シナリオ含む) |

| LULU-038 | 2026-07-15 | ✅ | **§22.15 改訂: 画像は「拡大」でなく「余白トリミング」— CSSズームで実装 + 検収合格(同ブランチ `fix/planner-arrival-snapshot-anchored`)**。ユーザー: 前案の拡大(`a036fa5`)は要望と違い、真の要望は**余白削減**。調査: 画像は **180×180 正方形**でキャラが中央に小さく周囲が透明余白(=画像内蔵)、正方形ボックスの `object-cover` では削れない。ユーザー選択=**CSSズームで詰める(画像加工なし)**。実装 `aa86ce5`: ボックスを元サイズに戻し(`MyCharacterCard` `w-20/md:w-24`、`CharacterDetail` 展開 `w-24/md:w-28`)、`<img>` を **`overflow-hidden` 固定サイズ div でラップ + 内側 img `scale-[1.3]`**(Tailwind4 は CSS `scale` プロパティを出力)。実機: カード container 102px に img 133px(scale1.3)を overflow-hidden クリップ / 詳細 119px 枠に img 155px クリップ / **横あふれ0**=透明余白の外周が枠外へ逃げてキャラが枠を埋める。`git diff origin/main -- CharacterDetail.jsx` は画像表示ラッパのみ(決定Aを画像表示の1点のみ緩和)・GroupPanel/CharacterGroupTools 空。**172テスト緑・build成功**。※最終の見た目(切れ具合)・scale 値微調整はユーザーのローカル確認(ブラウザペインの screenshot はタイムアウトで取得不可、DOM/computed style で機構を確証)。ブランチ全体=`f249a05`(§22.14 プランナー)+`a036fa5`(旧拡大・aa86ce5 に上書き)+`aa86ce5`(§22.15 ズーム詰め)。**push・PR・main マージはユーザー指示待ち** | 統括検収(実機・機構確証) |

| LULU-039 | 2026-07-15 | ✅ | **PR #12(§22.14 プランナー到達日=snapshot基準 / §22.15 画像余白トリミング)を [web-only] マージ + デプロイ + 本番検証=合格(ユーザー承認)**。push 前検証: `origin/main...HEAD` は **web 5ファイルのみ**(`CharacterDetail`/`CharacterPlannerTools`/`MyCharacterCard`/`rankingUtils`/`rankingUtils.test`)・対象外(ranking.db/bot/.github)混入なし(※`main...HEAD` はローカル main が PR#11 前で古く誤検出=`origin/main` 基準で確認)。172テスト緑・build成功・パリティOK。通常 push → **PR #12 作成**(https://github.com/pachimi14/maplen-board/pull/12)→ **[web-only] マージ `5730d4e`**。Pages workflow run `29354561622` = build/deploy/commit-db 全 success。**本番検証**: 新バンドル `index-dguUKXIy.js`(サーバは即更新・ブラウザ旧キャッシュは cache-bust で解消)で ①プランナー「Lv.255 約5日 **2026/7/18**」= データ7/13+N5(旧ビルドは7/19=キャッシュ)→ **到達日バグ解消** ②画像 computed `scale:1.5`・`translate:0 -8%` → 余白トリミング稼働。**オープンPR: なし**(PR#12 MERGED)。作業ツリー: `fix/...` は origin 同期・tracked clean。bot・朝のデータ更新に影響なし([web-only])。**T4b 関連の是正完了・本番稼働** | ユーザー承認(push・PR・[web-only]マージ)。本番検証合格 |

## 8. 共有インフラ(Gear Sim 統合・VPS・サイト全体)

> lulumi-tools.com 全体(EXPランキング+Gear Simulator+VPS)に跨る決定の**正は本節**。gear sim 側 DECISION_LOG からは参照のみ(正を2箇所に住まわせない)。

| ID | 日付 | 状態 | 決定 | 理由 |
|----|------|------|------|------|
| LULU-040 | 2026-07-16 | ✅ | **サイト全体アーキテクチャ(LULU-003 を部分置換)**: 静的配信は今後も **GitHub Pages が正**。VPS は「Pages にできない処理のみ」担当(T7 画像プロキシ / gear sim データAPI / 将来の公式APIキー管理)。**不変条件=「VPS が全落ちしてもサイト本体は表示できる」**を常に維持(VPS を静的配信・SPA配信の経路に入れない) | 静的サイトを VPS に移しても得る能力ゼロで運用負担のみ増える。北極星(毎朝の習慣)にはサイトが朝落ちていないことが必須で、Pages の可用性を手放さない |
| LULU-041 | 2026-07-16 | ✅ | **Gear Sim は同一オリジン・サブパス `/gear` で公開**(サブドメイン不採用)。vite `base: '/gear/'`。**公開方式=非公開ソースリポ→公開リポへの成果物(dist)push**: gear sim ソースは非公開リポで開発し、GitHub Actions(build)+deploy key で本リポの `gear/` 配下へビルド済み静的ファイルのみを push、既存 Pages デプロイに相乗り | localStorage はオリジン単位=サブドメインだと `maplen-board-*`(マイキャラ等)を gear sim から参照できず将来導線を殺す / GitHub Free は非公開リポの Pages 不可のため、公開するのは成果物のみに限定(生ソース・履歴・未公開機能は非公開のまま) |
| LULU-042 | 2026-07-16 | ✅ | **VPS API は `api.lulumi-tools.com` に集約**: DNS A レコード+リバースプロキシ(caddy or nginx)+Let's Encrypt で HTTPS 化(Pages は HTTPS のため http のままだと混在コンテンツでブロック)。**T7 画像プロキシと gear sim データAPI(catalog_server)を同一ドメインにパスで同居**(例 `/img/...`→画像プロキシ、`/gear/...`→catalog_server)。CORS 許可オリジンは `https://lulumi-tools.com` のみで開始(サブドメインは必要になるまで足さない) | TLS・ドメイン・リバプロ設定を1回で済ませ両機能のブロッカーを同時解消。画像プロキシは落ちても placeholder fallback でサイト無傷(LULU-040 の不変条件と整合) |
| LULU-043 | 2026-07-16 | ✅ | **作業順序(2レーン並行)**: ギアシミュレーン=PS-3c-1(発注済み計画)→PS-3c-2→PS-3c-3→実運用(現VPS単独のまま)。ランキングレーン=`api.lulumi-tools.com` HTTPS化(ユーザー手作業可)→T7完成。**T12(履歴書き換え)は両レーンの PR が全部マージされた静かなタイミング=統合直前**に実施(force push が in-flight ブランチを壊すため)。その後=統合(`gear/` 成果物push導線+サブパス公開)→GS-250。リポ統合(モノレポ化)は gear sim の UI がほぼ固まってから「移設のみ・挙動不変」の1PRで行い、開発中は統合しない | ユーザー承認(2026-07-16)。HTTPS化は T7 と gear sim 公開の両方のブロッカーのため最優先1回 / T12 は統合の前提(リポ肥大がビルド・clone を圧迫) |

| LULU-044 | 2026-07-16 | ✅ | **T7 の実装担当は Codex(GPT-5.5 high)を使用(ユーザー指定・PR-006 の本件限り例外)**。理由=Claude 週次トークン残 2% に対し Codex 残 98%。検収は従来どおり統括(Claude)が code-review+実機検証で実施し**作者≠レビュアーを維持**。計画書 §3 未決2点は推奨案で確定: ①スタック=Python+FastAPI+uvicorn ②placeholder=紺 `#0f172a`+緑 `#34d399` の汎用シルエット(ゲーム素材不使用)。パイロットラン成功済み(素curlで200/PNG、2026-07-16) | ユーザー指定 |

| LULU-045 | 2026-07-17 | 🔶 | **T7 検収=条件付き合格(コミット・push 前ゲート)**。Codex 実装(worktree `C:\tmp\msu-ranking-t7-img-proxy`、`t7/img-proxy`=origin/main+5コミット+テーマ回の未コミット差分)を code-review+直接検証で検収。**確認済み**: bot/`.github` 不触(diff 空)/ pytest 18 緑・web 全量 180 緑・build 成功 / CORS=完全一致 allowlist(既定 `https://lulumi-tools.com` のみ・env 上書き可・ワイルドカード無)/ `toShareProxyUrl`=ホスト+prefix 固定・パス検証・dev/prod 切替(`VITE_SHARE_IMAGE_PROXY_BASE` 上書き可)/ i18n 6ロケール 327 キー完全パリティ / 共有テキストの数値は既存 `rankingUtils`(getGainAmount/getGainRank/levelExpPercent)を再利用=正の重複なし・UI 表示と同一計算 / clipboard 不可環境の DL fallback・portal 化・`noopener` 確認。**仕様変更(ユーザー確認済みとして受理)**: 共有導線は詳細ページでなく**マイキャラカード側**(詳細からは ShareImageButton 撤去=共有はピン済みキャラのみ)・テーマ3種(fantasy/aqua/forest、`public/themes/*/frame.webp`≈144KB×3)・共有画像は英語固定・X は文+URL 方式。**push 前の残条件**: ①`frame.webp` 3点の出所確認(ゲーム/Nexon 素材・第三者著作物でないこと=LULU-018 と同方針) ②テーマ回のコミット分割(`.tmp_img_proxy_cache/` 除外・個別 add) ③PR-012 のユーザーローカル確認。VPS 配置(DNS/Caddy/systemd)と本番計測は push 後 | 統括検収(直接検証) |

| LULU-046 | 2026-07-17 | ✅ | **T7 テーマ画像3点の出所確認=受理(LULU-045 残条件①解消)**。ユーザー申告: fantasy=完全自作AI生成 / aqua・forest=ゲーム内スクリーンショットを参照画像に「雰囲気を似せて」AI生成。統括が3枚を目視検査: 実素材の複製・特定マップの再現・キャラ・ロゴ・UI 要素なし、汎用ファンタジー装飾調。焼き込みテキスト(EXP Growth Report / Lulumi Tools / MapleStory N EXP ranking)崩れなし。**forest(キノコの家=ヘネシス想起)が3枚中最高リスクだが識別可能な複製ではなく許容**。指摘が来た場合は forest から差し替え(テーマ構造は frame.webp 差し替えのみで対応可能な設計) | ユーザー申告+統括目視検査 |

| LULU-047 | 2026-07-17 | ✅ | **T7 共有導線=マイキャラカードのみで確定(ユーザー確定、LULU-045 の仕様変更を正式承認)**。詳細ページに ShareImageButton は置かない。根拠=共有機能の主目的は「自キャラの共有」。他人のキャラを共有したい場合はマイキャラ登録(サブ2枠・解除自由)で代替可能。ユーザーフィードバック次第で将来変更あり得る(その場合は詳細ページへの再設置=撤去済みコードの復元で軽微)。あわせて **PR-012 のユーザーローカル確認=完了済み**(LULU-045 残条件③解消)。残=コミット分割の確認→push 承認→PR→VPS 配置→本番計測 | ユーザー確定 |

| LULU-048 | 2026-07-17 | ✅ | **T7 コミット分割検収=合格(LULU-045 残条件②解消=検収全条件クローズ、push はユーザー承認待ち)**。`t7/img-proxy`=origin/main+8コミット(37f3422/ec9e57d/1c16e23/401f3aa/2e72f8e/54d6a35/640a18b/79c90d8)。境界=テーマ+アセット/UI+i18n+テスト/配線で単独revert可。キャッシュ混入ゼロ・`-w` 差18行は再インデントの実質差分(CRLF汚染なし)。**推奨デプロイ順: ①VPS 配置(DNS/Caddy/systemd、Codex 同梱例)→本番 curl で基準1〜4 計測 → ②push→PR→`[web-only]` マージ**。理由=プロキシ不達でも placeholder で機能は生存するが、プロキシ未配置のままマージするとキャラ画像なし共有が利用者に見える期間が生じる | 統括検収 |

| LULU-049 | 2026-07-17 | ✅ | **T7 img-proxy 本番公開完了(基準1〜4=本番実測合格)+ インフラ構成の事実記録**。ユーザーが**新VPS**に配置(旧VPS `163.44.97.148` は OOM 事故=SF cache bot が毎時 総VM2.1GB 要求→グローバルOOM→全ポート無応答→強制再起動、を受け分離。旧VPSの gear sim/bot 群は旧側のまま変更なし)。新VPS: `img-proxy.service`+Caddy(enabled・再起動後自動復帰確認済み)/ Let's Encrypt 証明書(期限 2026-10-14、Caddy 自動更新)/ HTTP→HTTPS 308 / 8781 は 127.0.0.1 バインドのみ / UFW 22・80・443 のみ許可。**本番実測**: `healthz` 200 / 実画像 200・image/png・4036B・0.109s / fallback 200+`X-Img-Fallback: 1`・0.299s / ACAO=`https://lulumi-tools.com`。DNS TTL 3600 のため最大約1時間はリゾルバにより新旧IPが揺れる(実害は伝播完了で消滅)。**LULU-042 の「api ドメインは旧VPSに同居」は事実上更新: api.lulumi-tools.com=新VPS、gear sim API の同居先も将来は新VPS 側を正とする(gear sim 公開時に再裁定)**。残=push 承認→PR→`[web-only]` マージ→本番で共有機能の最終確認 | ユーザー実施+統括受理 |

| LULU-050 | 2026-07-17 | ✅ | **T7 手順1〜4完了(実装担当 spawn・統括抜き取り検証済み)= PR #13 OPEN・マージはユーザー承認待ち**。①最終確認: web 180テスト全緑・build成功 ②差分: 35ファイル=web 21+server/img-proxy 14 のみ(bot/.github/キャッシュ混入ゼロ。実装担当報告の「34」は数え間違いで実害なし) ③`t7/img-proxy` を通常 push ④**PR #13**(https://github.com/pachimi14/maplen-board/pull/13)=OPEN/MERGEABLE、本文に「マージ時は `[web-only]`」注意書きあり。**マージ(手順5)はユーザーの明示許可が出るまで実行しない**。マージ後=統括が本番で共有機能の実機確認(手順6)→T7クローズ。備考: インフラ現況=新VPS 1GB に T7 のみ稼働・旧VPS 500MB に既存bot群(切替は別工程) | 統括(実装担当発注+抜き取り検証) |

| LULU-051 | 2026-07-17 | ✅ | **T7 完了(マージ・デプロイ・本番検証すべて合格)= クローズ**。①PR #13 本文の誤記訂正(Flask/gunicorn→FastAPI/uvicorn) ②マージ直前確認: OPEN/MERGEABLE/CLEAN・head 79c90d8 不変・35ファイル全てスコープ内・PR用CI無し(check-runs 0) ③**merge commit `79f7968`「T7: 共有画像生成 + 画像プロキシ [web-only] (#13)」**(2026-07-17 10:49 UTC・force無し) ④Pages workflow run 29574777039 = build/deploy/commit-db **全success** ⑤**本番実機検証(lulumi-tools.com)**: マイキャラ登録(Benjapol、profile-v1 生成)→共有モーダル(テーマ3種・実データプレビュー)→生成実行で **`api.lulumi-tools.com/img/charimages/...` が img+fetch 両経路で読込**(通常表示は market-static 直リンク維持=api請求は共有操作時のみ)/ **canvas 非汚染を本番プロキシ画像で直接証明(toDataURL 成功・180×180)** / **X intent URL をキャプチャ検証**(window.open スタブ・タブ非開・投稿なし: 文面/キャラURL(historyKey)/ハッシュタグ正・数値は画面表示と一致)/ コンソールエラー0。⑥手元→本番プロキシの基準1〜5も全合格済(healthz 0.089s・実画像 0.091s・ACAO 本番オリジン・fallback 200+ヘッダ・.jpg 400)。**未検証はペイン制約の2点のみ**: clipboard 書込の成否(ローカルでユーザー確認済)・スクショによる視覚確認(同)。残バックログ: 旧 `t7/share-image-spike` ブランチ+未コミットスパイクの後始末 / 既存bot群の新VPS切替(別工程) / T7-wire は本PRで接続済みのため不要 | 統括(マージ実行はユーザー明示承認 2026-07-17) |

| LULU-052 | 2026-07-15 | ✅ | **(2026-07-17 回収・正典化: 未コミットで孤児化していた本決定を、既存 LULU-040(2026-07-16・サイト全体アーキテクチャ)との ID 重複解消のため LULU-052 として末尾へ回収。決定日は原記録=2026-07-15)** **T6(キャラ個別ページ事前レンダリング+OG)=ドロップ確定 / 次タスク=T12(データ保存先移行+git履歴掃除)**。T6ドロップ理由: ①T7共有では添付画像が優先表示されOGカードは出ない(主要フローで出番なし) ②ハッシュルーティング(`#/character/`)は `#` 以降がクローラに届かずOG不可、機能させるにはパスルーティング化+全キャラ静的HTML生成の大工事 ③北極星(SNS流入)はT7が直接達成済み。→ 作らないリスト思考でドロップ。T12(時限〜2026-10)を次に着手。**注意: T12は履歴書き換え(force push=ユーザー専権)+CI変更を伴う破壊的・高トークン作業。実装は週次リセット後を推奨** | ユーザー決定(2026-07-15、2026-07-17 回収) |

| LULU-053 | 2026-07-20 | ✅ | **タスク順序 + T12 は統合計画として実施(分割しない)+ 実装事実の訂正**。①**順序**: T12(データ永続層刷新+git履歴整理)を先行実装、**Task Manager(TM)は計画のみ並行**(実装は別プロジェクトでユーザーが着手、統合方式=同一オリジン `/tasks` サブパス・localStorage `maplen-board-tasks-v1`・キャラキー historyKey・進捗は純粋関数=推奨B「試作→本体 `#/tasks` 移植」、計画書 `docs/IMPL_PLAN_TM.md`)。②**T12 は T12a-1 等へ分割せず、1計画書・1承認・1最終完了条件の統合計画で最後まで実施**(`docs/IMPL_PLAN_T12.md`)。内部フェーズ分割は可だが「一部完了で残りを後続送り」は禁止。スコープ=db.gz git コミット停止+**Release 単一Asset `db-store/ranking.db.gz` 耐久層**+**v2シャード復旧の実装**+v1 rankings.json 復旧経路廃止+旧処理/旧ファイル撤去+navigator/pages/retry 全workflow整合+**既存 1.9GB 履歴整理**+移行/復旧/競合/ロールバック試験+DECISION_LOG更新。③**訂正(統括の以前の誤説明)**: 「db.gz は navigator が単一書き手・pages は読むだけ」は**誤り**。実コード確認の結論=**pages(全件取得・rankings.json 唯一の発行者)と navigator(worldId 同期のみ・1b344cd で db-only 化=本番を発行しない)の両方が db.gz を書く**が、**blind 上書きでなく `merge_ranking_databases` の `INSERT OR IGNORE`=既存行を退行させない加算 union** で、かつ `concurrency: group=maplen-board-pages` で直列化。安全性は「単一書き手」でなく「加算マージ+直列化」で担保。過去の navigator 事故は**発行(rankings.json 上書き)**の問題であり db.gz merge が原因ではない。T12 設計不変条件: INV-1 発行は pages 単独/Release は配信経路に入れない・INV-2 Release書込は「download→additive-merge→--clobber」(blind clobber 禁止)・INV-3 加算マージ維持で両書き手・INV-4 concurrency 直列化・INV-5 v2シャード復旧の最悪ケース復元を実証してから v1 撤去・INV-6 履歴書換は静穏窓+CI停止+worktree再作成+ユーザー専権 | ユーザー決定(2026-07-20)+統括の実コード確認・自己訂正 |

| LULU-054 | 2026-07-20 | ✅ | **T12 P1(v2シャード復旧)検収合格・フェーズ完了(T12 は統合計画として継続)+ 復旧方式の裁定 + 三層防御 + 重複行バグ発見**。①**復旧方式=D 採用(方式A=percent逆算を撤回)**。方式A は実DB 51日で47%(158,011件)が過大復元(最大+20.5M)=精度不合格。D=後ろ向きexact(最新日 exact exp を起点に各日の exact `dailyGain`+exact level+`level_exp.py` で逆算)+破綻時のみ保守的fallback。**D の honest 特性(「完全に正確」ではない)**: 通常データ(99.48%)は **exact 復元** / exact連鎖が破綻(=既存の重複行アーティファクト等)した場合のみ**保守的な過小fallback** / **過大復元は構造的に0** / 実測で**表示文字列変化0件・現在順位入替0件**(丸め量子 `rankingUtils.js:124` に対し誤差が微小・破綻は真gain≤0の閑散日に局在し `dailyRank` は gain>0 のみ) / **fallback値は真値でなく欠損回避の保守的推定値**。②**冪等/additive ガード**: 復旧挿入前に既存 `(snapshot_date, character_asset_key)` を1クエリで取得しスキップ。空asset_keyは `name:<casefold>` にフォールバック。**2回実行 no-op・復旧後の(date,identity)重複0・過大0維持**を6基準で assert(pytest 71 passed)。opt-in 既定off(`SNAPSHOT_IMPORT_FROM_V2_SHARDS`)・取得ロジック(LULU-004)不触。③**重複行データ品質バグを発見**: `ranking_snapshot` の `UNIQUE(snapshot_date, rank)`(identity単位でない)+ 初期再取り込みで、40 identity・6日・167超過行が発生。`analysis.py` の (date,identity) 上書きで**本番v2 に偽/負の dailyGain スパイクと同日二重描画が現在も出ている**(105セル)。→ **別 IMPL_PLAN `docs/IMPL_PLAN_dq-dup-rows.md`(承認待ち)**。**三層防御=復旧側(冪等ガード=本P1)/出力側(`analysis.py` 集約=P-DQ-1)/DB側(部分UNIQUE制約=P-DQ-3)**。ブランチ `t12/p1-v2-shard-recovery`(**未push**)commits 7ba8f59/4257f91/4524045/15e87a5/2dbbac7 | ユーザー裁定(D採用・冪等ガード・別タスク起票 2026-07-20)+統括検収(実測・71 passed) |

| LULU-055 | 2026-07-20 | 🔶 | **ブートストラップ期の未検証復元データ発見 + 日付別の扱い裁定(6/1の信頼性判断は継続中)**。P-DQ-1 の11セル負値調査で判明: **`ranking_snapshot` の 2026-05-29(82.9%)・06-01(99.5%)が本物のAPI取得でなく、丸めた `levelExpPercent` からの逆算復元**(class/job空・単独行で機械判定可、計 **10,174行 / 5,590 identity**)。**全て回収不能**(DB他行・git履歴に pre-import なし・v1/v2は同一DB由来・seed に不在=4系統確認)。11の負 dailyGain はこの丸め誤差が「見える方向」に出た症状で、残り約1万は静かに紛れた未検証の正値。集計影響は現状ゼロ(5/29〜6/3 は現行 週次07-13〜/月次06-20〜 の窓外)だが90日履歴チャートには表示。**裁定(ユーザー 2026-07-20)**: ①**正式本格運用開始 = 2026-06-01。2026-05-29 はテスト段階**→**当日を全行削除・正式履歴の対象外**(推定行の選別残しでなく日付全体を除外)。削除前に **backup / 対象行数 / 削除後v2差分 / 05-30以降の dailyGain 影響**を確認。②**06-01 は本格運用開始日のため日付全体の削除は非承認**。5,598復元行の**実測値としての信頼度・06-02以降との連続性・誤差規模・欠損化した場合の影響**を**別途精査中(本エントリ 🔶=6/1判断継続)**。③**11の負値**: 5/29起因は全削除に包含、6/1起因は個別削除・欠損扱いを維持。④**05-30/05-31 は保持(ユーザー裁定=A)**: 正式運用前だが**大半が実API取得の genuine データ=信頼できる参考履歴として保持**、削除しない。**「正式履歴開始=06-01」は運用マイルストーンの表現であり、05-30/05-31 の genuine データ削除を意味しない**(誤読防止)。日付の位置づけ: 05-29=テストデータ削除 / 05-30・05-31=正式運用前の参考履歴だが信頼できる実取得として保持 / 06-01=本格運用開始日。05-29 削除で計算不能になる **dailyGain(05-30) は 0/推定でなく null・欠損**扱い。05-30/05-31 の累積EXP・level・順位の実測スナップショット自体は削除しない。⑤重複由来の偽スパイクは P-DQ-1 集約で解消済み(別軸)。**P-DQ-2 の削除対象(現時点)= 05-29 全スナップショット + 既確定の余分な重複行のみ。06-01 は調査結果まで一括削除・補正しない** | ユーザー裁定(2026-07-20)+統括の実データ確認(調査担当・実DB検証) |

| LULU-056 | 2026-07-20 | ✅ | **P-DQ-1(出力側集約)本番反映完了・実機検証済み(夜間・ユーザー①承認範囲=非破壊の検収通過は反映可)**。`dq/analysis-aggregation` を main へマージ(merge `32091c0`・`[web-only]` で fetch非発火の deploy-only 再ビルド誘発)→ pages run **29724317259 = success**。**本番 v2 実機検証**(lulumi-tools.com/data/v2、latestSnapshotDate=2026-07-19): VeryAlone00 の **06-02 dailyGain = 1,437,515(真値)= 偽の +6.2B スパイク解消**、real な +6.2B は正しく **06-04** に再帰属 / **同日重複point なし** / 負・>100B の dailyGain なし。三層防御の**出力側(層2)が本番稼働**し、重複行由来の偽/負スパイクを停止。DB不変・可逆(revert可)。**未完(朝の承認待ち)**: P-DQ-2 の DB削除実行(dry-run/backup 準備済み `dq/repair-p-dq-2`・`--execute` は起床後) / 06-01 の扱い(起床後裁定・夜間不変) / T12 P6・Release移行開始・本番DB削除(専権・自律実行せず) | 統括(夜間・ユーザー①承認)+本番実機検証 |

| LULU-057 | 2026-07-20 | ✅ | **06-01 裁定 + 「負dailyGain→null」原則ルール + 3責務分離実装(検収済み・DB削除は最終dry-run確認後に承認)**。**裁定(ユーザー 2026-07-20)**: 06-01 の exp/level/rank/履歴点は**保持**、`dailyGain < 0`(累積EXPは増加のみ=ドメイン上不成立)は**日付限定せず一律 null**(0/推定で埋めない)。**3責務を別コミット・別検証に分離**: ①**P-DQ-2(DB修復)** `dq/repair-p-dq-2`——05-29 全行(5,544)+確定重複超過(123、91組)= **5,667行削除**(現DB dry-run・backup済・可逆・**未実行**)②**bot出力防御(負→null)** `dq/negative-gain-null` commit `d250308`——`analysis.py` で負dailyGain→None(58 passed)③**Web UI(null欠損表示)** 同ブランチ `484d720`——`GainChartTooltip` の null を「+0」でなく既存キー `characterDetail.noData`(6ロケール327キー全パリティ・新規キー不要)、チャートは gap(180 web tests)。**最終dry-run 3件数**(現DB): DB削除=**5,667** / 出力null化=**733**(05-29削除後・06-01=356/06-02=374/06-03=3。05-30の2,297は前日05-29消滅で「前日なしnull」へ吸収)/ UI「—」=**8,392**(90日窓)。**partial所見(要判断・未対応)**: `sum_daily_gains_in_period` は null日を単純スキップ=「gain=0の実日」と「null(不明)日」を含む期間が同値を返し区別不能(`test_partial_period_total_is_indistinguishable_from_complete_total` で実証)。現行は影響なし(対象日は現行7/30日窓外)だが将来の負値でも起きるためpartialフラグの要否は要判断。実装担当は独断でUI仕様追加せず報告のみ。**DB削除(P-DQ-2実行)は最終dry-run確認後のユーザー承認まで実行しない** | ユーザー裁定+統括検収(実データ・pytest 58/web 180) |

| LULU-058 | 2026-07-20 | ✅ | **P-DQ-2 実行完了 + 日付シフト・インシデントと復旧 + 移行marker の恒久修正**。①**P-DQ-2 実行**: 05-29 全行(5,544)+確定重複超過(123)= **5,667行削除**・VACUUM(38.6→35.0MB)。backup 復元で内容ハッシュ完全一致=可逆を実証。②**インシデント(統括の操作起因)**: 削除を定着させる目的で Actions cache(`maplen-board-data-v4-*`)を全削除したところ、**cache のみに存在した移行marker `.ranking_day_label_utc_gain`(`exp_ranking/bot/.gitignore` 対象)が消失** → `apply_ranking_day_label_migration`(marker 不在なら**無条件で全 snapshot_date を -1 シフト**)が再走 → **重複行 +7,676・spurious 05-29(5,558=05-30のシフトコピー)が本番に混入**。※統括は当初「全日付が-1にずれた」と誤診したが、実測(同一キャラの exp 一致・日付別カウント比較)で**正データは無傷・余分な重複行が乗った**状態と判明。Navigator が毎回フル26分同期したのも同 cache 削除で同期状態が消えたため。③**復旧**: clean db.gz(`0b88c38`)へ復元 + **marker を `git add -f` で追跡化(恒久修正)** + 汚染 cache 削除 + **`[web-only]` で再デプロイ**(汚染された公開v1の再importを無効化)。④**検証(全合格)**: run ログに **Shift/migration 記録なし=marker が効き再走せず** / origin/main db.gz **05-29=0・336,164行(クリーン値と完全一致=重複ゼロ)**・05-30/31/06-01=5558/5562/5628 保持 / 本番v2 **05-29 不在・負dailyGain ゼロ・snapshotCount 336,164 一致**。⑤**恒久修正の効果**: marker が git 追跡化されたため、**cache の evict/削除でも日付シフト移行は二度と再走しない**(朝の定時 run も安全)= 既存の地雷を除去。⑥**当初目的は全達成**: P-DQ-2削除 / 負dailyGain→null(一律・0や推定で埋めない)/ UI「—」欠損表示 / 06-01 と 05-30/31 の保持 | 統括(実行・インシデント・復旧)+実測検証 |

| LULU-059 | 2026-07-20 | ✅ | **P-DQ-3 完了(三層防御の完成)+ partial は別 IMPL_PLAN へ後回し(統括裁定)**。①**P-DQ-3 本番反映済**: ユーザー裁定(案a)= **2026-06-01 の日付は保持**し同一 `(snapshot_date, character_asset_key)` の**余剰行のみ削除**(20組・**37行**、全て 06-01)+ **部分UNIQUE index `idx_ranking_snapshot_date_asset_key`(`WHERE character_asset_key != ''`)を作成**。正行規則は `analysis.select_canonical_snapshot_row` を再利用(正は1箇所)。migration は `init_db`/`main.py` に**未配線**=CI で自動発火しない安全既定。**検証全合格**: 336,127行(-37)・**重複0組**・index 存在(commit-db 往復後も保持)・05-29=0 維持・06-01=5,591(日付保持)・**本番v2 count 336,127 でDBと一致**(見え方は P-DQ-1 集約により不変)。**→ 三層防御完成**(出力側集約=P-DQ-1 / DB修復=P-DQ-2・3 / DB制約=部分UNIQUE)。②**partial(不完全期間フラグ)は今回実装せず、別 IMPL_PLAN へ**(統括裁定・アドバイザー助言+統括の実測追認)。**根拠**: 「負→null を入れたから partial も必要」という因果は**事実と異なる**——期間集計は初日から**「観測できた日の合計」**という意味論で、**現行7日窓で 7,657 identity 中 195(2.5%)が既に7日未満**のスナップショットしか持たない(圏外落ち・新規225到達・取得漏れ)。partial は既存の穴であり負→null はそこに1経路足しただけ。**急造フラグは新たな不誠実を生む**(「null gain 日のみ partial」だと欠落日195件が complete のまま)。残る発火経路は genuine API 異常のみで**実測0件/約50日**、しかもその場合**翌日の gain が対で過大**になるため partial 単体では誠実に扱えない。**発火条件(記録)**: 現行7/30日窓内に null gain 日または新たな負値が観測されたら partial プランを最優先で起票。**別プランで解く設計未決5点**: (1)partial の定義域(スナップショット欠落日を含むか)(2)新規参入キャラの扱い (3)genuine 異常時の隣接日過大値 (4)既存2.5%へのUX影響 (5)7/30/90日それぞれへの適用。**検収の定型手順に「現行窓内に null/負値が入っていないか確認」を追加**(コード変更なし) | ユーザー裁定(P-DQ-3=案a)+統括裁定(partial 後回し)+実測検証 |

| LULU-060 | 2026-07-20 | ✅ | **T12 P1+P2 本番着地完了(Release 耐久層が稼働開始)= P2→P3 観測ゲート入り**。①**着地**: `t12/p2-release-store`(P1+P2)を現 main へ rebase して merge `aa04027` → push → pages run **29738132846 全 success**。rebase 時のコンフリクト1件(`sqlite_storage.py` に main の P-DQ-3 関数群と P1 の v2シャード復旧関数群が同位置追加)は**両方保持**で解決。P1 受け入れテスト1件が失敗したが**コード欠陥でなくテスト前提の陳腐化**——「重複日注入で fallback 発火」の前提が **P-DQ-1 の出力側集約により重複がシャードに到達しなくなった**ため。**トリガを null dailyGain(main の負→null 由来)へ更新**し、`conservative_fallback > 0` は維持しつつ **`< DAYS//4`(局所性)を追加=強化**。「重複注入は P-DQ-1 集約で fallback を起こさない」ことを新テストで明示記録。**117 passed**。②**Release `db-store` 作成・初回シード成功**: prerelease・asset `ranking.db.gz` 37.85MB。persist 結果 `{'downloaded': False, 'merged_rows': 0, 'uploaded': True}`=初回シードとして正しい。**アセット実DL検証**: 336,127行・**05-29=0**(P-DQ-2 維持)・**重複0組**(P-DQ-3 維持)・**index `idx_ranking_snapshot_date_asset_key` 保持**・最新日 2026-07-19 = 現DBと完全一致。③**挙動不変を維持**: v2シャード復旧は opt-in 既定 off / git db.gz の日次コミットは継続(**両系並行**)/ rankings.json 発行は pages 単独(INV-1)/ concurrency 直列化(INV-4)/ blind clobber 禁止・fail-closed(INV-2)/ 加算マージ再利用(INV-3)。④**次=P2→P3 完了ゲート**: pages≥3回・navigator≥1回の正常完了で、Release版とgit db.gz版が `ranking_snapshot` 総行数・snapshot_days・最新日・最新日行数・`character_meta`(総数/world_id充填)・`app_meta.last_ranking_fetched_at`・整列内容ハッシュで**全一致**することを確認するまで P3(出血停止)に進まない。**watch: persist ステップは `continue-on-error: true` のため失敗が静か**——観測期間中はログ確認が必須 | 統括(着地・検収・実測検証)+ユーザー承認(2026-07-20) |

| LULU-061 | 2026-07-21 | ✅ | **ホヨンの職業名不具合を修正・本番反映(+ T12 P2 の完全サイクル動作を確認)**。**症状(ユーザー報告)**: ホヨンの職業が "Anima Thief" と表示され職業ソート/ランキング/職業内順位から欠落。**根本原因(実データで確定)**: API はホヨンを **`JobCode_ANIMA_THIEF4`/`ClassCode_ANIMA_THIEF`** で返すが `bot/job_names.py` の `JOB_DISPLAY_BY_BASE` に **`ANIMA_THIEF` が未登録**(既存の `"HOYOUNG"` エントリは **DB に一度も出現せず空振り**)→ 汎用フォールバック(`_`→空白+`.title()`)で `job="Anima Thief"` として出力 → web(`jobCategories.js`)は "Ho Young" を Anima 系に登録済みだが "Anima Thief" は未登録のため分類漏れ。**回帰でなく未対応ケース**(2026-07-10 の最初の225+到達から・5人)。**クラス網羅**: `DARK_KNIGHT`(204人)/`BLADE_MASTER`(34)/`CANNON_MASTER`(14)も未登録だが**フォールバック結果が偶然正しい**だけと判明。**修正**: `"ANIMA_THIEF": "Ho Young"` を追加(必須1行)+ 上記3件も明示登録(**表示名は不変**)+ **未知 job_code でフォールバックした際に `logger.warning`**(API 新職業コードを早期検出=同クラス欠陥の構造的予防)+ 本番30種を網羅する回帰テスト。**DB変更不要**(`job` は `job_code` から導出のため再エクスポートで過去日も自動修正)。**検証**: 統括実測で**全30種中29種が修正前と完全一致・変化は `ANIMA_THIEF4` の1件のみ**(回帰なし)、**122 passed**。merge `2aae271` `[web-only]` → run 29848036519 全success → **本番実測: "Anima Thief" 残存0・"Ho Young" 5人**。②**副産物**: この run の Release persist が **`{'downloaded': True, 'merged_rows': 0, 'uploaded': True}`** = **初回シード後の「DL→加算マージ→--clobber」完全サイクルが本番で正常動作**(INV-2 の実運用確認)。③**運用上の注意(watch)**: 複数エージェントが**同一ワーキングツリー**で並行 `checkout`/`commit` するとリスクがあるため、今後は worktree 分離を検討 | ユーザー報告→統括調査→実装担当→統括検収(実測)+ユーザー承認 |

| LULU-062 | 2026-07-27 | ✅ | **EXPテーブル20%減(2026-07-23)への era 対応: ①公開中の誤表示を修正・本番反映済 ②v2シャード復旧の era 対応(B')検収合格**。**発端**=ユーザー報告「過去の増加量がおかしい」。**根本原因**: `calculate_progress_toward_275` が in-level exp を**現行(20%減)テーブルの必要値でクランプ**し全履歴に適用。`db0ed3d` は旧テーブルを `LEGACY_EXP_TO_NEXT_LEVEL_PRE_2026_07_23` として残したが**gain 本経路が日付非対応**でデッドコード化していた。**確定事実(実測)**: パッチは 2026-07-23 07:40 UTC に必要EXPを**正確に20%減**(比0.8000)/ 境界は **snapshot_date ≤ 07-22 が旧表・≥ 07-23 が新表**(fetch 時刻で確認)/ パッチで**絶対expは保持**(境界の同レベル継続 6,269人全員 exp 増加・減少0)/ **ドメイン仕様(ユーザー提供)= テーブル改定後は次の exp 獲得までレベルアップ処理が保留**(超過滞留→1exp獲得で level 上昇+exp 正規化。Navigator の% も初回ログインまで旧値表示)。**被害**: pre-patch の日次gain **20.2%(70,993/351,512)が誤り**(最大 −817B の過小)+ **wake-up 日に最大1,744倍の過大**(MostLi8Dark 07-24 表示+142.3B/真値+81.6M・46件/2日・計+2.14T)がランキングを汚染。**①修正(merge `79ef279` `[web-only]`・本番検証済)**: クランプを外し**行き先日のテーブルで差分を取る単一式**へ(`gain(d)=P(T_d,level_d,exp_d)−P(T_d,level_{d−1},exp_{d−1})`、非クランプ)。同レベル→raw exp差(厳密)/ era内レベルアップ→厳密 / **wake-up→繰越を自動処理で過大が構造的に消滅** / 境界跨ぎレベルアップ(1,474人)→**保守的下限で過大ゼロ**。**EXP% は再計算した真値を表示(100%超もそのまま・実測上限124.774%=理論値125%(1/0.8)に一致・125%超0件・レベルの捏造はしない)**(ユーザー裁定)。**DB無変更**(gain は導出値=再エクスポートで過去日も自動修正)。**検証**: 同レベルペアが raw exp差と **0/374,863 不一致** / wake-up 46件の過大全件解消 / pachimi **07-04=+575,570,820,446・07-13=+436,805,319,389**(本番実機でも一致・負値0)/ bot 140 passed・web 297 passed・6ロケール不変。**②B'(merge 承認待ち)**: ①の副作用で **v2シャード復旧が単一テーブル逆算のため dailyGain を過大復元**(統括の独立検証で **27,372/359,271=7.62%・最大3.07倍**、Benjapol 07-20 1.73倍)= T12 P1 の「過大復元しない」保証が破れた。**ユーザー指示により①を先に最速出荷**(逆順だと era対応復旧が旧意味論シャードを誤読するため**この順序が安全**)、該当3テストは理由付き `xfail`(strict)+ゲート既定OFF維持で凌ぎ、直後に B' を実施。**B' 設計**: progress アキュムレータを捨て**リンク単位の exp 算術** `exp_prev = exp_curr + Σ T_d[level_prev..level_curr−1] − gain(d)`(同レベルなら**テーブルが式から消える**)+ **ガードを旧表上界 `0 ≤ exp_prev ≤ LEGACY[level_prev]` へ再設計**(現行ガードは wake-up の正解を拒否していた=B案の穴)+ fallback も era 対応 + `exp_table_for` を **(発効日,テーブル)リストへ一般化(意味論は一般化しない。新era追加時は3不変条件を実測再検証)** + `expTableVersion` 未知時 warning + method census ログ。**統括が実装前に数学を証明**(373,022点で不一致0・過大0)。**検収(統括の独立ハーネス)**: **過大 27,372→0・過小 13,969→0・exact 317,930→374,121(全点ビット完全一致)**、Benjapol 07-20 が真値と一致。145 passed・**xfail 0**・web 不触。代替案は却下(null化=値が導出可能なのに捨てる LULU-057 の誤用・形式変更=`snapshotDate` で era が決まるので冗長) | ユーザー裁定(%方針・出荷順序・スコープ拡大)+アドバイザー助言+統括の独立検証 |

| LULU-063 | 2026-07-30 | ✅ | **T12 P3(出血停止)完了クローズ + P4(v1完全廃止)完了**。①**P3**: 3 workflow の復元元を git db.gz → **Release `db-store`** へ、**db.gz の日次 git コミットを停止**(permissions write→read も伴った)、**Release persist の `continue-on-error` を除去**、**`SNAPSHOT_IMPORT_FROM_V2_SHARDS=true`** で v2 層を有効化、**【新規】切り詰め公開防止ガード**(cache/Release/v1/v2 の復元完了後・API取得/export/persist の前に実行。下限=**公開v2 `meta.snapshotDays` → 直近正常Release → 絶対30日**、**実基準が両方取れなければ fail-closed**〈統括の検収指摘で是正〉。行数は固定値でなく **0.90 の割合**判定。発火時は **Pages更新なし・Release上書きなし**)。**反映後に2件の障害を検知・即修正**: (a)`commit-db` の permissions を read に下げたため **`gh release upload` が HTTP 403**(耐久層が一時停止)→ `contents: write` へ戻す(`bb275aa`)。**`continue-on-error` を外していたおかげで即座に検知できた** (b)main.py 実行 step に **`GH_TOKEN` が無く guard の Release baseline が取得できず実基準が1本だけ**→ 追加(`5f79a4b`)。**実測(9項目)**: Release から復元 / v2 は通常時 no-op / **db.gz コミット0件** / persist 成功 / snapshot_days 連続 / 公開v2 と Release DB 一致 / **retry も `release-db-restored=True` で Release 経路** / **リポ増加 +40KB/2日(従来 約200MB/日が停止)** / ガードが `basis: v2_public=60 release=60 floor=30` の**二重実基準**で作動。**増加原因の訂正**: 「突然200MB/日」は不正確で、実データでは**平常時 86〜136MB/日**(3〜4コミット × 29→45MB と blob 肥大)+**デプロイ集中日は 370〜385MB/日**。**TM統合(2,704行)・EXPテーブル修正(238行)の寄与は無視できる**(KB規模)。②**P4**: **v1 `rankings.json`(64,346,028バイト)の生成・配信・復旧利用を全廃**。`/data/rankings.json` は **404 にせず 145バイトの廃止案内 JSON**(`deprecated`/`replacement`/`retiredAt`)へ置換(ユーザー裁定。案内ファイルの扱いは **P7** で再判断)。v1 専用コード撤去(`hydrate_character_meta_from_url/json`・`import_missing_snapshots_from_url`・`export_mvp_json`・config 4関数・`inject_dummy_gains.py`・`export_mvp_json.py`)。**復旧チェーンは cache → Release → v2シャード → cold** に確定。**worldId 事前実証 W1〜W4 全成立**: Release復元後 **7,982/7,982** / **空DBに本番v2から復元しても 7,953/7,953(= v2 は worldId を保持)** / 全消去しても navigator `sync_world_ids` が `always_refresh_missing=True` で **1 run 全件回復** / 廃止案内JSONに対する旧hydrateは**完全no-op** → **worldId 供給は二重経路で確保**。**取得・リトライ・スキップ判定はバイト単位で無変更(LULU-004)**。**本番実測(10項目)**: 全 workflow success / 廃止案内 200・145B / v2+shards 正常 / **公開v2 と Release DB が days=60・rows=406,612・latest=2026-07-28 で一致** / **worldId 100%** / persist 成功 / db.gz 0件 / **約61.4MiB 削減**。③**P7 の整理候補として記録**: `IMPORT_SNAPSHOTS_JSON` / `data/seed/rankings_seed.json`(5.8MB・対象日は本番DBに既存) / config のシード関連設定 / `import_snapshots_from_mvp_json` の最終的な参照整理(**v1 とは別のコールドスタート用シード機構**から参照が残るため P4 では残置=実装担当の停止報告を統括が裁定) | ユーザー承認(P3/P4 反映・v1 URL の扱い・worldId 事前実証の要求)+統括検収(実測) |
| LULU-064 | 2026-08-05 | ✅ | **スターフォース強化費用 履歴チャートを新設**(設計正典=`docs/DESIGN_SF_COST_HISTORY.md` r2、ブランチ `feat/sf-cost-history`、スライス SH-1〜SH-6・全て統括が独立検証して検収)。ランキングとは独立の第2の入口。公式 dynamicpricing の1時間足を150日分 VPS に保存し、4時間足に集約して配信。**現在価格は公式 latest をサーバーが TTL60s でプロキシ**(ブラウザから公式を直接叩かせない=CORS/レート制限/ユーザーIPの露出。img-proxy と同じ理由)。**Expected 費用はブラウザで都度計算**(実測 900点=32ms)。①**API は本番稼働**: `api.lulumi-tools.com/sf-history/*`(`127.0.0.1:8785`・`sf-history.service`・`botuser`)。Caddy は **snippet + handle 1ブロックの純粋な追加**で、既存 `/img/*` `/exp/*` `/notifications/*` `:8783` `gear.*` は body まで照合して回帰ゼロを確認(**reload であり restart していない**=`ActiveEnterTimestamp` 不変)。バックアップ `/etc/caddy/Caddyfile.bak-20260805-sfhistory`。差分ジョブは **ワンショット+timer**(01,05,09,13,17,21:43)で、手動1回実行の実測=**616リクエスト全200・429ゼロ・10分58秒・メモリピーク31.8MB**。②**計算式は移植であって正ではない**: 正は maplenEnhancebot `packages/engine/src/starforce.ts`。型注釈を落として vendor し、**向こうの parity fixture 14装備×2,180 span を越境 golden として vitest で照合**。統括は**fixture 外の乱数400ケースでも本家エンジンとビット完全一致**を独立確認(相対差 0)。検証なき同期面は作らない。③**Expected のみ・分位を出さない**(maplenEnhancebot GS-269=analytic 分位は経験則スケーリングで、実分位と混在する)。**恒久判断**。**ΔCP も出さない**ため GS-254/GS-242 の影響を構造的に受けず、あちらの公開保留と独立に出せる。④**実データで判明した設計変更3件**: (a)**装備ごとに☆上限が違う**——6装備は☆20 止まりで、UI が目標星を制限しないとチャートが全 null になり「壊れている」と映る(`maxStar` はデータから導出・ハードコード禁止)。(b)**保持期間は150日でなく最低160日**、かつ**窓を広く要求するほど少なく返る**逆転現象(P1 は SH-1 の単一条件からの誤った一般化だった)。(c)**☆20/21 の同値は API のクランプではなく実挙動**(全28装備で時系列では分岐・完全一致0装備)=U6 解決。⑤**Worker/キャンセル/キャッシュは作らない**(実測32msで不要=作らないリスト)。⑥**応答は小数2桁丸め**(gzip 127KB→86KB)。Expected への影響は**実測 ≤3.4e-10**(26.3億に対し0.24 NESO)で承認。DB は full precision のまま=**この非対称を README に明記**。**★検収で捕えた P0**: SH-5 は `npm run test` 354 passed・build 成功だったが**ブラウザで真っ白**だった(`defaultPresetForMaxStar` が `{from,to}` を返し、**truthy ゆえ `!range` ガードをすり抜け**、React がルートごと unmount してランキング画面も巻き添えで死ぬ)。**純粋関数テストでは原理的に捕まらない欠陥**であり、緑は証拠になっていなかった。修正後にブラウザで再検証。**★未了(完成ではない)**: **push していない=画面は未公開**(ユーザー専権・「完成してから最後に」)。**→ LULU-065 で本番公開して完了**。 | ユーザー(設計 r2 承認・設置先とVPS利用の指摘・SH-6 までの自律実行の明示委任・公開は最後と裁定)+統括(全スライスの独立検証・SH-6 は自ら実施) |

| LULU-065 | 2026-08-06 | ✅ | **`Enhance History` を本番公開してプロジェクトを一時完了**(LULU-064 の「★未了」を閉じる)。①**プレオープン → 本番の2段構え**: まず VPS `sf.lulumi-tools.com` に出し(**アクセス制限は掛けず「リンクを張らないだけ」**)、身内フィードバックで **SH-7〜SH-31** を回してから公開。**この形が有効だった** — 実機フィードバックでしか出ない指摘(足のラベルが区間開始か終了か・破線が2本に見える・パーセンタイルの意味が伝わらない・時刻をローカルで見たい)が大量に出た。②**公開**: PR #17 を **`--no-ff` + `[web-only]`** のマージコミット `d5b2212` で main へ。run `31028942480` build/deploy/commit-db 全緑。**★`[web-only]` は必須**: `maplen-board-pages.yml` は `SKIP_RUN_IF_RANKING_DAY_EXISTS: true` のため、付けないと `deploy=false` で **Pages に反映されない**。**fast-forward では効かない**(ワークフローが見るのは末尾コミットのメッセージ)。既存 `744b0f6`/`79ef279` と同じ慣行。③**既存サイトへの影響を事前実測**: 共有ファイルの変更は `App.jsx`/`useHashRoute.js`/`BoardHeader.jsx` の**加算のみ**、バンドル **+46KB(+4.3%)**(1,067,232→1,113,475。recharts は既存サイトが既に使用)、CORS は `https://lulumi-tools.com` が既に許可済み、秘密情報の混入ゼロ。公開後の実測でも **ランキング側 7,999 characters・`updatedAt` 不変**=`[web-only]` が意図どおり効いた。④**命名**: ナビは **全言語共通の英語 `Enhance History`**(他のナビ項目が英語のままなのに SF履歴だけ翻訳されていた不整合の是正 + **将来キューブ価格を扱うと "SF" が嘘になる**)。⑤**時刻表示**: UTC を主・ローカルを従の2重表示。ゾーン略称は **夏時間が無く略称が世界で一意なゾーンのみ**の静的表(`JST`/`KST`)で、それ以外は `UTC±HH`。**`CST`(中国/米中部/キューバ)・`IST`(印/以/愛)は意図的に入れない**。`Intl` の `timeZoneName:"short"` は**書式化ロケール依存**(`Asia/Tokyo` は `ja` でのみ `JST`)なので不採用 — **閲覧者の UI 言語と居住タイムゾーンは無関係**。⑥**上流データの制約(仕様ではない)**: 星ごとに履歴の開始日が違い、チャートは**必要な全ステップが揃った足からしか描けない**。Dreamy Belt は ☆20/21 が 5/4 始まりのため **5/4 開始**(他は約160日)。**データを発明しないので埋めない**。⑦**★反映経路が変わった**: 以後 UI 修正は **main への push → Pages** が本番の反映経路(プレオープンの VPS 手動転送は別系統として生存)。⑧**残**: 375px 幅でナビ `Enhance History` の右端が画面端に密着(横スクロール・文字切れなし。**未修正・軽微**)。es/th/vi/zh-TW の暫定訳は**ネイティブレビュー不可のため完成扱い**(ユーザー裁定)。 | ユーザー(プレオープン運用の裁定・全フィードバックの発信・公開指示・マージ実施・完了宣言)+統括(SH-7〜SH-31 の計画/検収・事前実測・公開後の実機確認) |
| LULU-066 | 2026-08-07 | ✅ | **取得上限8,000件による静かなデータ切り捨てを解消 + 2026-08-06分を救出**。**不具合**: `RANKING_MAX_PAGES=800` × `API_MAX_PAGE_SIZE=10` = **8,000件**が実人口を下回り、**2026-07-29 に初到達して以降 8日連続で Lv225+ を取りこぼしていた**(07-29〜08-02・08-05・08-06 が 8,000ちょうど / 08-03・08-04 が 7,999)。**上限到達時に警告が出ない**(ループは「空ページ」「10件未満」では `logger.info` を出すが **`max_pages` を使い切ると黙って抜ける**)ため**1週間気づけなかった**。公開 v2 も `characterCount=8000` で切り捨て済み配信。**修正**(merge `9ce1b63`): ①**`RANKING_MAX_PAGES` を 800→2000(=20,000件)**(workflow + `config.py` 既定値)。**完全撤廃はしない** — ループの自然終了が「空ページ/10件未満」依存のため API 異常時に無限ループになる。増加ペース約25件/日で十数年分の余裕 ②**上限到達を `logger.warning` で可視化**。**取得ループの外(戻り値受領後)で判定**するため **`main.py` の取得・リトライ・スキップ判定は削除行0の純追加のみ(LULU-004 遵守)**。挙動は変えず run も失敗させない(部分データでも保存する方が欠測より良いため、**気づける状態にすることが目的**)。**検証**: bot **175 passed**(既存173+警告の発火/非発火2)/ YAML OK / `git diff -w` で `main.py` は純追加のみ。**救出**: 公式EXPは UTC 00:00 更新でリセットまで同一ランキング日が返るため、`force_fetch=true` の手動 dispatch で **2026-08-06 を再取得 → 8,000 → 8,247 件(+247人)**。加算マージ(`INSERT OR IGNORE`)で既存行は不変、P-DQ-3 の部分UNIQUE制約が二重登録を構造的に防止。**cap警告0件=実人口を取り切れた証拠**。公開 v2 も `characterCount=8247` へ更新。**復旧不可**: **07-29〜08-05 の7日分**は API が過去日を返さないため**永久欠測**(各日 約247人分)。**運用上の発見(実運用で判明)**: **`maplen-board-pages` の concurrency group を Pages と Navigator が共有**しており、**push → Pages → Navigator の `workflow_run` 連鎖でキュー待ちの run が後発に追い出される**(`cancel-in-progress: false` でも pending は cancel される)。実際に最初の救出 dispatch がこれで cancel された。**手動 dispatch は実行中/キューが空になってから行うこと** | ユーザー報告(上限に気づく)+ユーザー承認(上限引き上げ・警告追加・救出実行)+統括の実測検証 |
| LULU-109 | 2026-08-13 | ✅ | **①-b(Pages 先行起動バッファの拡張)の効果を6日間観測して確定 + 冗長 cron が初週で実際に本番を救済**。**背景**: 設計は「JST 8:00 先行起動 → ランナー上で JST 9:05 まで待機(`jst_schedule.wait_until_jst_fetch_window`) → 45秒間隔で公式更新を検知 → 即取得」で、**設計バッファは 65分**。しかし GitHub のスケジューラ遅延が**中央値 +58分(消費率91%)**でこれを食い潰しており、**2026-08-04 は +68分で既に3分超過**(誰も気づかず)、**2026-08-07 は +161分**で JST 10:40 到着=**取得窓の95分後**となり、待機・ポーリング機構が完全に空振りした。データ正確性はベースライン比較方式のため無傷だが、**北極星である「毎朝の習慣」の速度が失われた**。**cron は遅れるだけでなく真にドロップする**ことも実証(`lulumi-ranking-retry.yml` は cron 3本に対し毎日2本しか発火せず、schedule run 20本すべて success で cancelled 0 = concurrency eviction ではない)→ **単一 cron の前倒しでは不足で冗長化が必須**。**対策(merge `3c93eb9`, PR #19)**: cron `0 23`(1本)→ **`7 20`(JST 5:07・余裕238分) + `7 22`(JST 7:07・余裕118分)の2本**に置換し `0 23` は削除。`jobs.build` に **`timeout-minutes: 330`** を明示。**bot は無変更**(待機・ポーリング機構は正しく動いており原因ではない)。**分値7は毎時0分の混雑ピーク回避**、**`0 23` 削除は**(a)余裕65分が `7 22` の下位互換 (b)JST 8:00 発火が朝の push run を evict する窓を作る、の2点による。**アドバイザー裁定=修正付きGO**で、統括案から4点修正(分値0→7 / 受け入れ基準「2本目は約20秒」は skip 判定が `main.py` 内で checkout〜pip が必ず走るため**物理的に不可能**→「≤5分・fetch ログ無し」へ是正 / 「push 静か未デプロイ」watch-item の追加 / LULU-066 の先行マージ)。**6日間(08-07〜08-12)の実測=全基準クリア**: `7 20` は **6/6日発火・ドロップ0・遅延 中央値+50分/最大+52分**(予算238分の**22%**。修正前は91%消費) / `7 22` も **6/6日発火**・最大+48分 / **取得窓マージン 185分**(基準60分以上。**修正前は −95分**) / 前倒し取得なし(`JST fetch window open` 後に取得) / **`snapshotDays` 69→75 で6日+6=欠落0**・`characterCount=8428`・cap警告0。**★冗長化が初週で実際に効いた(想定外の理由で)**: **2026-08-12 の `7 20` run が failure**。原因はログで確定 — 公式 API が **JST 9:00 リセット直後の「再構築中」状態**を返し、`wait_for_ranking_update` は「1ページ目がベースラインと違う」ため**更新と誤判定**(probe 8)、settle 45秒後の本取得が **20件(3ページ目が空)** となった。これを **`validate_ranking_freshness`(`changed=20 < required=100`)が fail-closed で拒否**して run を落とし、**切り詰めデータの公開を防いだ(正しい動作)**。pending だった **`7 22` の run が 8,428件を正常取得して当日分を確保**。冗長 cron は「ドロップ日の保険」として入れたが、**別クラスの障害(API の一時的不完全応答)で初週から本番を救った**。**方針=A(記録して様子見)をユーザーが裁定**: 多層防御(freshness guard + 冗長 cron)が設計どおり働き実害ゼロのため、1件の観測で `wait_for_ranking_update` の settle 延長やポーリング条件強化(bot の取得周辺=LULU-004 近傍)には**触らない**。**watch**: 「更新検知 ≠ データ再構築完了」の再発頻度。**残 watch-item(①-c で解消予定)**: concurrency group `maplen-board-pages` の占有が **約30分 → 毎日 JST 約5:53〜9:31 の約3.6時間**に拡大。この間に `exp_ranking/**` を push すると pending が evict され、後続の schedule run は `SKIP_DEPLOY_IF_DAY_CAPTURED` で `deploy=false` のため **push した修正が静かに未デプロイになる**。暫定運用ルール=**その時間帯に push したら Actions を目視し、run が消えていたら `workflow_dispatch` で再実行**。 | ユーザー体感報告(「今朝スケジュールのPagesが動いてなかった」)→統括の debug-strategy 調査(遅延分布の実測・ドロップの実証)→アドバイザー裁定(修正付きGO)→ユーザー承認(cron変更=専権事項)→統括の6日間実測検収 |
| LULU-112 | 2026-08-17 | ✅ | **T12 P5.5(Google Drive 世代バックアップ)完了 → P6 の開始条件が成立**。**目的**: P3 で db.gz の日次 git コミットを廃止して以降、完全DBの永続層は **GitHub Release `db-store` の単一アセット**だけになった。P6(履歴書き換え)でその前提をさらに固める前に、**GitHub の外にある独立した退避先**を確立する(§0 の3層: Actions cache=高速化 / Release=永続層の正 / **Drive=世代退避**)。**実装(PR #26, merge `a180c18`)**: 新ジョブ `backup-gdrive` を pages/navigator 両方に追加(`needs: [build, commit-db]`)。**設計の要は「Release Asset を唯一の受け渡し媒体にする」**(§2.1.1) — `needs:` はジョブ順序を決めるだけでファイルは渡らないため、**commit-db が persist 直後に Asset を再取得して SHA-256 を job output に記録**し、backup-gdrive が同じ Asset を再ダウンロードして照合する(**V0=バックアップ対象がその run で公開したDBと同一であることの証明**。artifact 経由にしないのは、二重の受け渡し経路による乖離の余地を無くすため)。**処理順は §2.1 厳守で「削除は最後」**: V0 → gzip/SQLite 健全性 → upload → **Drive から再取得して** V1〜V7(存在/非0/サイズ一致/SHA-256一致/gzip/SQLite/統計突合) → **全通過後にのみ**保持ポリシー適用。**保持**: 直近7 UTC日(8日以上前のみ削除)・同日複数世代は最新のみ・**「新規+直前の正常世代」を下回る削除計画は実行しない**。**全失敗モードが fail-closed**(アップロード失敗/検証失敗/一覧取得失敗/ファイル名パース不能 — いずれも削除せず run を赤くする)。**秘密情報**(§2.6): `${{ secrets.* }}` 参照のみ、例外は全箇所 `type(exc).__name__` の要約で生メッセージを出さない(統括が raise 全5箇所を目視 + `str(exc)`/`{exc}` の漏洩経路 grep 0件を確認)。スコープは **`drive.file`**(アプリが作成したファイルのみ)。**検収(統括の独立実測)**: bot **234 passed, 1 skipped**(基準175から減なし)/ tools 11 passed / YAML 3本 OK / **既存 bot ファイル(main.py・release_store・snapshot_guard・sqlite_storage)は差分0(LULU-004 遵守)**、既存 workflow の変更は commit-db への `id` + SHA-256 出力追記のみで persist ロジック自体は無変更。**受け入れ基準は §3 全12項目達成**。**★保存先をメイン Google アカウント → サブアカウントへ切り替え(ユーザー判断)**: client_id/secret は「アプリの身分証」でしかなく保存先を決めない(保存先=**同意画面でログインしたアカウント**)ため、**GCP は無変更のまま、ローカルで同意し直して refresh token と folder ID の2件だけ Secrets を更新**すれば移行できる。この切り替えで **2回連続で本番が赤くなり、その2件がそれぞれ別クラスの失敗を実証した**: ①**有効な token + 見えない folder ID**(新旧の食い違い。`drive.file` はサブ垢の token からメイン垢のフォルダを見られない)→ **アップロード段階で HttpError** ②**失効した refresh token**(統括が案内した「アクセス権の取り消し」をサブ垢側で実行した可能性/コピー欠け)→ **認証段階で RefreshError**。**いずれも削除は一切走らず、公開データ・Release も無傷**=fail-closed が本番で2パターン実証された。**教訓: 4つの Secrets はワンセットで、token と folder ID は必ず同一実行のペアを登録する**(混ぜると壊れる)。3回目で解消し以降安定。**運用実績**: 2026-08-15〜08-17 に **7 run 連続成功**(要件3を超過)。**保持ポリシーは本番でも実削除が作動**(run `31987342633` で同一UTC日の重複排除により1件削除)。**★復元ドリル(§3-12)を 2026-08-17 に実施・合格**: ユーザーがサブ垢の Drive Web UI から実際にダウンロードし、統括が検証 — **D1 SHA-256 が CI 記録値と完全一致**(`4829e002a844…` / 67,057,127バイト)/ **D2 gzip 展開可**(293,720,064バイト)/ **D3 `PRAGMA integrity_check = ok`**・テーブル4種確認 / **D4 本番 Release Asset とバイト完全一致**(=Drive 世代は Release の完全な複製。復元は書き戻すだけで足りる)/ **D5 統計6項目一致**(総行数563,043・snapshot_days 79・最新日2026-08-16・最新日行数8,550・character_meta 8,550・worldId充填8,550)。記録は `docs/T12_P5_5_RESTORE_GUIDE.md` §6。**書き戻し自体は実行しない**(正常な Release を同一内容で上書きするだけで状態が変わらず、失敗時のみリスクがあるため。経路は CI が毎日実行しているものと同一)。**残 watch**: ①**経過日数(8日以上)による削除**は世代が溜まる 2026-08-23 頃に自然発生する見込み(合成テストでは実証済み) ②SKIP日/`[web-only]` run でも backup-gdrive が走る(同日重複排除で安全な no-op だが Drive 転送が余分。実運用でコスト懸念が出れば完全スキップへ変更可) ③OAuth アプリは **In production 済み**(Testing のままだと refresh token が7日で失効するため) ④メイン垢側の旧フォルダ・アクセス権の後片付けは未実施(ユーザー作業。**取り消し時はアカウントの取り違えに注意** — ②の失敗の原因になりうる)。**→ P6(履歴書き換え・1.9GB回収)の開始条件チェックリスト5項目すべてが充足**(`docs/T12_P6_RUNBOOK.md` §2)。 | ユーザー(GCP/OAuth 設定・Secrets 登録・保存先のサブ垢移行判断・Drive からの実ダウンロード)+実装担当(stage1/stage2 実装)+統括(計画・検収・独立検証・復元ドリルの検証) |
| LULU-113 | 2026-08-17 | ✅ | **T12 P6(git 履歴書き換え)完了 = T12 の主目的を達成**。**ローカル 3.35 GiB → 48.34 MiB**(目標 <400MB)。**手順**: 開始条件(P3/P4/P5/P5.5 完了)を満たしたうえで、①**全 worktree のクリーン確認**(4つとも追跡変更0)②**dangling commit 24件と未マージ2本に backup タグ26個を付与**(bundle は ref 到達分しか保存しないため。stash は0件で、dangling は全て main に別 SHA で存在することを抜き取り検証済み)③**bundle 作成**(`C:/tmp/maplen-board-PRE-P6.bundle` 3.4GB・`verify`=complete history)④`git gc`(**`--prune=now`/`--aggressive` なし**=ユーザー指定。dangling を巻き添えにしないため)⑤`fsck --full` 健全 ⑥**CI 3本を disable**(静穏窓14.6時間を確保)⑦worktree 3つを退避 ⑧**リモートブランチ17本削除**(全て `merge-base --is-ancestor` で main 内と検証。**残すと旧履歴を到達可能に保ちサイズが減らない**)⑨**Step 3.5**: `ranking.db.gz` の追跡解除を通常 push(`33f6ec6`)。**`bot/.gitignore` の `!data/ranking.db.gz` という否定ルール**(明示的に追跡へ戻す指定)を削除して逆に無視対象へ ⑩**filter-repo**(単一ファイル取得=案A、システム未汚染)で `ranking.db.gz`/`ranking.db` を全履歴から除去(1.7秒)⑪検証 ⑫**ユーザーが force push** ⑬CI 再開 ⑭事後検証。**検証結果**: db.gz blob **109個 → 0個**(main 履歴)/ **ルートツリー `62b79602…` がリモート main と完全一致**=内容が1バイトも変わっていない証明 / web `7719a11d…`・bot `1e643255…` のツリーも不変 / `fsck --full` 健全 / bot **234 passed, 1 skipped**・web build 成功。**書換後の初 run `32001217557` は build/deploy/commit-db/backup-gdrive 全 success**、**`release-db-restored=True`(293,720,064 バイト)**=db.gz が履歴から消えても Release 永続層が機能、guard は `basis: v2_public=79 release=79` の二重実基準で作動、**本番 v2 は `latest=2026-08-16 / count=8550 / days=79` で完全に無変化**。**Release `db-store` も無傷**(`ranking.db.gz` 67,057,127バイト・`state: uploaded`・更新時刻不変)。**★アドバイザー(外部レビュー)の指摘で手順を改善**: 統括案の「plain `--force` を2回」は **main だけ成功しタグが失敗する中間状態**を生み、それこそ旧履歴が到達可能なまま残る P6 最悪の失敗形だった。**`git push --atomic --force-with-lease=<ref>:<期待SHA>` で main と `db-store` を同時更新**する形に変更(統括も dry-run で両 ref の更新可否を独立確認)。**★`db-store` タグの同時更新は必須**: Release のタグが旧コミットを指したままだと旧履歴が GC 対象外になる。**Release アセットは Release オブジェクトに属する**ためタグ移動で消えない(実測確認)。なお **db.gz のみを変更した旧コミットは filter-repo が空コミットとして刈る**ため、タグは生き残った直近の祖先へ再マップされた(旧 `b16d790e` "chore: update ranking.db" → 新 `8df5ebc` "Merge T12 P1+P2"。**統括はこの旧コミットの正体を確認せず推測で述べる誤りを犯し、レビューで訂正された**)。**★ランブックに危険な記述があった**: Step 6 の **`git push --force --tags origin`** をそのまま実行していれば `backup/pre-p6/*` 26本が公開されていた(実測では書換済みで db.gz は0個のため旧履歴の復活は起きないが、main に無い177オブジェクトを remote に残す)。**本エントリと同時に本文を修正**。**★実施中に踏んだ罠2件も手順書へ反映**: (a)**書換後の検証で `git fetch` を使ってはいけない** — 旧履歴3.3GBを再取得し、しかも中断してもバックグラウンドで完走する。内容同一性は **ルートツリーSHA の照合**(`gh api …commits/main --jq .commit.tree.sha` vs `git rev-parse HEAD^{tree}`)で足り、diff より厳密で転送も発生しない (b)**`gc --prune=now` だけでは減らない** — **reflog が旧 main を掴み続ける**ため。実測 48MiB→(fetch)→3.39GiB→(gc のみ)→3.35GiB→(**`reflog expire --expire=now --all` + gc**)→**48.34MiB**。**★リモートのサイズは減らない(重要)**: 実測 3,514,447 KB → 3,514,544 KB(微増)。原因はランブック §4-1 の「GC 遅延」だけでなく、**`refs/pull/*/head` が31本あり旧コミットを到達可能に保っている**こと。これは **GitHub 管理の ref で利用者は削除できない**ため、**リモートの実縮小には GitHub Support への GC 依頼が必須**(「遅延」ではなく「依頼しない限り永久に減らない」)。**【2026-08-17 訂正】この判断は誤りだった** — 数時間後の実測でリモートは **4,310 KB(4.2MB)まで自然に縮小**していた(PR refs 自体は残るが `.size` に計上されない)。**依頼は不要**。詳細は LULU-114。**残 watch**: ①db.gz の新規コミットが0/日であることを数日後に再確認 ②`refs/codex/turn-diffs/...`(Codex が作った checkpoint ref)が db.gz を1個・45.3MiB 掴んでいる。filter-repo は tree を指す ref を扱えず skip した。**push 対象外でリモートには無影響**だが、ローカル48MiBの大半がこれ。**統括が作ったものではないため削除は保留**(ユーザー判断) ③bundle・backup タグ26個は**検証完了まで削除しない** ④GitHub Support への GC 依頼を出すか否か。 | ユーザー(force push の実行=専権事項・外部レビューの手配)+アドバイザー(atomic/force-with-lease への改善・`--tags` 禁止・統括の事実誤認の指摘)+統括(準備・全検証・実施後の記録と手順書修正) |
| LULU-114 | 2026-08-17 | ✅ | **T12 完了(P1〜P7 全フェーズ達成) = リポジトリ 3.35 GiB → 4.45 MiB(99.87%減)、リモートも 3,514,447 KB → 4,310 KB(99.88%減)**。**P7(最終掃除)の内容**: ①**シード機構の撤去**(本体)。`data/seed/rankings_seed.json`(5.6MB・2026-05-31/06-01 の2日分のみ)+ `IMPORT_SNAPSHOTS_JSON` + `config` の3関数 + `sqlite_storage` の `import_snapshots_from_mvp_json`/`snapshot_dates_in_mvp_json` + README のシード節 + 退避 bat を撤去。**撤去の根拠**: 復旧チェーン `cache → Release db-store → v2シャード` が確立済み(P6 後の初 run で `release-db-restored=True` を実測)/ v2シャード復旧は era 対応で過大復元ゼロ(B')/ Drive 世代バックアップが独立層として稼働 → **シードが救うシナリオは既に3層で覆われている**。加えて**シードの2日分は 2026-08-29/30 に retention(90日)で消える運命**で、放置すると「retention が消す日付を毎 run 入れ直しては消される」チャーンになるため期限前撤去が自然だった。②**実装担当の停止報告を統括が裁定(A=現状維持)**: 計画書の行番号リストに無かった `ranking_day.py` への波及(一回限りの UTC gain-day 移行が `config.snapshot_seed_json_path()` を参照していた)。**勝手に進めず報告してきたのは正しい対応**。裁定根拠=この移行は `.ranking_day_label_utc_gain` マーカー(**日付シフト事故 LULU-058 の再発防止で git 追跡化した 213バイトのファイル**)により二度と走らずガードされている / 変更は JSON シフトのループを空にしただけで SQLite 行シフト・skip マーカーシフト(移行の本体)は無変更。B(関数だけ残置)は「呼び出し元が消えた関数を基準達成のためだけに残す」ことになり次の読み手を混乱させるため却下。③**統括の検収で追加の死にコードを発見・同時撤去**: 実装担当の報告に無かったが `shift_mvp_json_file` が呼び出し元ゼロになっており、連鎖して `shift_mvp_json_dates`・`shift_chart_date`・`test_ranking_day_migration.py`(全1テスト)も「JSON 内の日付をシフトするためだけ」の存在になっていた。**対象 JSON は v1(P4 廃止)とシード(P7 廃止)の2つだけで、もうシステムに存在しない**ため、チェーンごと撤去(未使用 import の `json`/`Any` も)。`shift_iso_date` は SQLite 側で現役のため残置。④pytest の一時DB(`_test_*.db`)を gitignore。**検証**: bot **233 passed, 1 skipped**(削除した1テスト分のみ減少・機能テストの巻き添えゼロ)/ web build 成功 / YAML 3本 OK / **LULU-004 不触**(`main.py` の差分は `bootstrap_database` と import 文のみで fetch/retry/skip 判定に差分ゼロ)/ Release persist・snapshot guard・v2シャード復旧・backup-gdrive に**差分ゼロ**/ **cold start が v2シャードのみで成立**することを合成テストで実証(`test_p3_restore_fallback_gate.py` 3 passed)/ シード関連5関数の残参照 0件。**★統括の誤りを実測で訂正(重要)**: LULU-113 で「`refs/pull/*/head` が31本あり旧コミットを到達可能に保つため、**リモートは GitHub Support に依頼しない限り永久に減らない**」と記録したが、**数時間後の実測でリモートは 4,310 KB(4.2MB)まで縮小していた**。PR refs 自体は32本残り旧コミットも API から取得可能なままだが、**GitHub は GC を実施しており、PR refs 経由のオブジェクトは `.size` に計上されない**模様。**「永久に減らない」は誤り** — 実際には**放置で 99.88% 減を達成**した。ユーザー裁定の「Support へ依頼しない」は結果的に正解だった。**T12 受け入れ基準(`docs/IMPL_PLAN_T12.md` §5)全9行の最終状態**: ①bot 233 passed ✅ ②web build 成功 ✅ ③v2シャード復旧の実証 ✅(B' で 374,121/374,121 ビット完全一致)④additive-merge の退行なし ✅ ⑤**db.gz コミット 0/日**(P3 以降 **0件**)✅ ⑥v1 完全廃止 ✅(145バイトの廃止案内のみ・**現状維持を裁定**)⑦**リポサイズ回収 < 400MB** ✅(**ローカル 4.45 MiB / リモート 4,310 KB**。起点は計画時 1.92GB だが実際は 3.35 GiB まで肥大していた)⑧本番挙動不変 ✅(`latest=2026-08-16 / count=8550 / days=79`)⑨復旧の実運用実証 ✅(`release-db-restored=True` 293,720,064 バイト)。**後片付けの裁定(ユーザー)**: ①**GitHub Support への GC 依頼=しない**(結果的に不要だった)②**`refs/codex` は B のみ削除**(2026-08-13 のチェックポイントが `ranking.db.gz` 45.3MiB を単独で生かしていた。**A=当日作成分は main に無い blob が0個で削除しても1バイトも減らないため保持**)。削除後 **48.34 MiB → 4.45 MiB・db.gz の到達可能数 0個**。再発リスクなし(P6 Step 3.5 で db.gz が追跡外になったため今後のチェックポイントに入らない)③**bundle(3.4GB)と backup タグ26個は自動で消えるまで放置**(`C:/tmp` は OS が消しうる場所だが、それで構わないとの裁定)。**残 watch**: 孤立ディレクトリ `msu-ranking-sfhist`(main の複製・固有データなし)の削除は任意。 | ユーザー(P7 承認・後片付け4件の裁定・force push の実行)+実装担当(P7 実装と停止報告)+統括(計画・検収・死にコードの発見・自らの誤りの訂正) |


## 7. 軽微な整理・改善候補(バックログ・非緊急)

- **フィルタ適用状態の可視化(T2 ローカル確認で判明した UX 改善要望・不具合ではない)**: URL からフィルタ付きで開いた場合、フィルタパネルが折りたたまれたままで「なぜ絞り込まれているか」が初見で分かりにくい。候補=①有効なフィルタが1つでもあればフィルタパネルを自動展開 ②折りたたみ時も「○件のフィルタ適用中」バッジ表示。T2 本体はこのままで問題なし(ユーザー承認)。今後の改善タスク候補
- **お気に入り/グループの historyKey 移行 + プロファイル層統合(独立後続タスク)**: 既存 favorites/groups は `character.name` 依存(改名で消失、LULU-005 違反)。将来、name→historyKey の**安全な一度きり移行**(旧データ保全・移行失敗時の消失防止)を設計し、`maplen-board-profile-v1` へ統合する。T4 では**やらない**(既存データ消失リスク回避のため分離)。参照 LULU-024
- **BoardHeader の未使用 `loadError` prop**(QW=LULU-016 で表示から外れた)。`App.jsx` の `<BoardHeader loadError={loadError} />` と `BoardHeader({ loadError })` の受け渡しが未使用。将来の軽微リファクタで prop を削除してよい(QW スコープ外のため保留)

## 6. 次に閉じるべき未決事項(優先順)

1. **T0 の状態管理方針の承認**(選択キャラ・フィルタの持ち方)。承認後 IMPL_PLAN_T0 を確定 → implementer へ
2. **web テスト基盤の初期化**(現状 web は vitest なし)。roadmap 方針では T3 の派生統計モジュールの純粋関数にだけ vitest を入れる。T0 時点では build 成功のみが受け入れ基準
3. T1(`mvp_export.py` 拡張)のデータ契約: rankFluctuation・前日レベル順位・職業内/サーバー内順位の JSON 形状を確定
4. LULU-009(T12 保存先移行)の実施タイミングと移行先(Releases / 外部)の決定

| TM-066 | 2026-07-22 | ✅ | **ダッシュボードのマイキャラ登録はランキング共有プロフィールから分離し、dashboard schema v3の`characterHistoryKeys`（最大2体）を正とする**。登録ダイアログではランキング側マイキャラを読み取り専用候補として検索欄の前に表示し、1クリックで追加できる。ダッシュボード側の追加・削除はランキング側プロフィールを変更しない | ユーザー指定。ランキング側の既存登録を再利用しつつ、各ページの登録目的と削除責任を分離するため |

## 9. Raffle Calculator（2026-08-01）

| ID | 日付 | 状態 | 決定 | 理由 |
|----|------|------|------|------|
| LULU-067 | 2026-08-01 | ✅ | **Lulumi-Toolsのサイトスコープを「EXPランキングサイト」から「MSUプレイヤー向けツール群」へ明示的に拡張する**。LULU-001のEXPランキング北極星はランキング領域の獲得・習慣化指標として維持し、Task Manager・Raffle Calculator等を暗黙の例外にしない | 既に複数ツールを同一SPAへ統合しており、正典と実態の乖離を解消するため。ユーザー承認 |
| LULU-068 | 2026-08-01 | ✅ | **Raffle Calculator仕様`docs/SPEC_RAFFLE_CALCULATOR.md`を承認**。公開名=`Raffle Calculator`、route=`#/raffle`、ナビ順=`EXP Ranking / Task Manager / Raffle Calculator`。初期版は抽選済み履歴の分配のみ。Power Crystal 1対NESOの換算レートはユーザー指定、コイン単価は`prizes`にコインが記録されたキャラクターごとに指定し、単一コイン種の検証通過を条件とする。Incomplete・UNKNOWNはfail-visibleで自動計算しない | MapleHubのコードやロジックを流用せず、キャラクター別コイン価格と安全な不完全データ処理を実現するため。ユーザーが§20推奨案を全承認 |
| LULU-069 | 2026-08-01 | ✅ | **MSU APIキーはGitHub Pages、クライアント、Git、fixture、ログ、実装担当へ渡さず、新VPS上の独立`raffle-api`プロセスだけが環境変数から読む**。ブラウザは`api.lulumi-tools.com/raffle/v1/*`だけを呼び、VPS停止時も静的SPAと他ツールは表示する。wallet addressと生assetKeyも運用ログへ残さない。会話内に提示済みのsandbox keyは本番相当運用前にローテーションする | GitHub Pagesでは秘密を保持できず、LULU-040/042/049とMSU公式のキー保護要件を満たすため |
| LULU-070 | 2026-08-01 | ✅ | **ラッフル分配計算の正はWeb側の純粋関数、raffle-apiは取得・検証・正規化のみ**。レスポンス契約は`schemaVersion`、報酬分類は`classificationVersion`を持ち、同一匿名化fixtureをWebとサーバーの両テストで共有する。実装計画前にPR-013前提監査として`raffled_at`規則・`winCount`対応・コイン種別・新VPS 1GBの容量を最小検証する | 二重実装と同期面の無検証増加を防ぎ、外部API前提の誤りを最安段階で検出するため。ユーザー承認済み仕様の実装ゲート |
| LULU-071 | 2026-08-01 | ✅ | **Raffle Calculator初期版の分配対象をLucid・Willに限定し、「コイン」はArachno Coin・Phantasma Coinだけを指す**。両コインの個数はキャラクターごとに合算し、prizesに対象コインが記録されたキャラクターへ既承認のキャラクター別単価を適用する。Florin、AbsoLab Coin、Stigma Coinその他のExchange Currencyと、他ボスのクリアは分配対象外として警告・除外する | ユーザーによる対象範囲の明確化。API取得範囲に複数通貨が混在しても、ドメイン上の対象2コインだけを明示分類することで誤混入を防ぐため |
| LULU-072 | 2026-08-01 | ✅ | **分配計算はボスごとに完全分離する**。LucidではPhantasma Coin、WillではArachno Coinだけを扱い、対象ボスを1つ選択してキャラクター別単価・Power Crystal換算・残高・送金一覧を計算する。Arachno CoinとPhantasma Coinの合算、ボス間の単価共有、残高相殺は行わない。価格設定もLucid用とWill用を分けて保存する | ユーザーによる分配単位の明確化。LULU-071の「両コインを合算して同一単価」の部分を置き換え、ボスごとの独立精算を保証するため |
| LULU-073 | 2026-08-01 | ✅ | **docs/IMPL_PLAN_RAFFLE_CALCULATOR.mdを承認し、codex/raffle-calculatorで実装を開始する**。前提監査に依存しないroute・BigInt計算・ボス別storage・fixture mode API骨格から進め、実APIの報酬正規化はprizes/clearInformation・winCount・Power Crystal・対象ボス/コインの匿名化fixtureが確定するまで停止する | ユーザーが「既に承認」と明示。実装計画の安全ゲートと、未検証API前提を推測しない停止条件を同時に維持するため |
| LULU-074 | 2026-08-01 | ✅ | **Raffle ResultsはLucid・Willに限定せず、公式APIから取得できた抽選済みラッフル結果を全ボス分表示する。分配、価格入力、Party ClearsはLucid・Willだけに限定する**。API契約は表示用`raffleResults`と分配用`clears`を分離し、その他ボスを表示から欠落させず、分配へも混入させない | MapleHubと同様に履歴確認機能として全結果を見られることが必要というユーザー確認。表示範囲と分配範囲を別概念として固定するため |
| LULU-075 | 2026-08-01 | ✅ | **PT履歴は直近の木曜日00:00 UTCの公式開催回を一括取得する。Raffle ResultsはPT内で選択した1キャラの全当選結果、Party Clearsは保存PT全員が同じ構成で倒したLucid/Willだけを表示する**。分配はボスタブ内でコイン・装備・ボスNESO・Power Crystal・Ascendant NESOをチェック選択し、Power Crystal初期比率は1、コイン/装備は取得者のドロップ1件ごとに売却総額を入力する。事前コイン単価と日時入力は廃止する | ユーザーによる業務フローの再定義。表示、共通クリア判定、分配設定の順序をMapleHub相当の利用目的へ合わせ、週1回ボスのため同一ボス複数クリア集約を不要と確定したため |
| LULU-076 | 2026-08-01 | ✅ | **PTメンバー追加は、VPSバックエンドによるMSU公式APIのキャラクター名完全一致検索とNavigator URL入力の2方式に限定する**。名前検索はSuggest APIの候補を完全一致で絞り、Characters APIで正規化済み詳細を確定する。raw asset key直接入力とランキング候補選択は提供せず、assetKeyは内部の安定識別子としてだけ保持する。MSU APIキーはVPS環境変数からのみ読み、ブラウザへ渡さない | ランキング対象外も名前で追加でき、利用者へ内部識別子を要求しないため。公式のマーケット検索は出品中キャラクターに限定されるため、一般検索の正には使用しない |
| LULU-077 | 2026-08-02 | ✅ | **Party Clearsの部分PT一致を5-of-6限定から一般化し、登録2人以上の全員が同一開催回・同一Lucid/Will・同一公式partyCount・clearedAt差60秒以内で一致すれば候補表示する**。公式partyCountが登録人数より多い場合は「討伐時はN人ですが、登録済みM人で分配計算しますか？」と明示確認し、承認後だけ登録メンバーで計算する。登録1人のみ、公式partyCountが登録人数より少ない、履歴不一致は候補外とする | ラッフル不参加者が複数いる場合や、API上で取得できるメンバーが2〜4人だけの場合にも分配を行えるようにするというユーザー指定。人数差を無確認で計算しない安全ゲートは維持するため |
| LULU-078 | 2026-08-02 | ✅ | **分配用Ascendant履歴はボスclear時刻ではなく、同一開催回の公式ボス難易度と承認済みTier対応表で結び付ける**。Lucid Easy＝Dawning 2、Normal＝Mystic、Hard＝Divine、Will Easy＝Luminous、Normal＝Glorious、Hard＝Eternal。Ascendant履歴の`clearInformations`は実データで空のため時刻近傍方式を廃止し、対象Tierが0件または複数件なら推測で合算しない | 5人Normal Willで旧実装がBoss NESO 8.1Mしか計上せず、GloriousのAscendant NESO 42MとPower Crystal 36Mを欠落させた。Tier対応後は換算率1.0で86.1M、1.1でMapleHubと一致する89.7Mになることを公式履歴で確定したため |
| LULU-079 | 2026-08-02 | ✅ | **Party Clearsのタブと分配設定見出しでは、Lucid / Willのボス名だけでなく公式難易度と対応Ascendant Tierを一体表示する**。表示形式は例として`Normal Will + Glorious Ascendant`。APIの各clearには`bossDifficulty`と`ascendantTier`を必須化し、6通りの承認済み対応表と不一致ならWeb契約で拒否する | 分配対象のBoss NESOとAscendant NESO / Power Crystalがどの難易度・Tierの組かを利用者が一目で検証でき、誤ったTier対応を表示層でも検知できるようにするため。ユーザー指定 |
| LULU-080 | 2026-08-02 | ✅ | **Party Clearsの同一PT判定から`clearedAt`差の制限を廃止し、登録全員の同一開催回・同一Lucid/Will難易度（layerId）・同一公式partyCountだけで一致判定する**。登録PTはユーザーが明示的に作成し、各ボスは開催回ごとに1回しか討伐できないため、時刻による追加推定は行わない。部分PTは従来どおり登録2人以上かつ公式partyCountが登録人数以上の場合に候補表示し、人数差を明示確認してから分配する | 同一Hard Will・partyCount 6の4人で`clearedAt`が最大約2分35秒ずれ、60秒制限により正しいParty Clearが欠落した。`clearedAt`は実討伐時刻ではなく参加券使用等の時刻である可能性があり、同一PT判定の根拠として不適切とユーザーが裁定。LULU-077の時刻条件を置き換える |
| LULU-081 | 2026-08-02 | ✅ | **保存PTを分配対象者、ラッフル履歴があるメンバーを獲得情報の提供者として分離する**。同一開催回のLucid / Will履歴がPT内の1人以上にあれば難易度別候補を生成し、履歴がないメンバーも獲得0で保存PT全員の均等分配へ含める。`partyCount`と`clearedAt`は候補の除外条件にしない。同一ボスでHard / Normal等が混在した場合は候補をチェックで複数選択可能とし、1件選択は単独、複数選択はBoss NESO・Power Crystal・Ascendant NESO・コイン・装備売却額をメンバー別に合算して1回の分配を行う。LucidとWillは従来どおり別精算 | ユーザーがPTを分配対象として明示入力しているため、履歴欠落を理由にメンバーを分配から除外しない。実例の6人討伐・ラッフル履歴4人・6人分配に対応しつつ、同一ボスの複数難易度混在はユーザー選択で誤合算を防ぎ、必要なら両方の合算も可能にする。LULU-077およびLULU-080の全員一致・partyCount一致条件を置き換える |
| LULU-082 | 2026-08-02 | ✅ | **Party Clearsの同一PT判定へ公式PT人数とラッフル参加時刻を戻す**。候補は同一開催回・同一Lucid / Will難易度で、各履歴の公式`partyCount`が保存PT人数と一致し、複数人PTでは2人以上の`clearedAt`が最古から最新まで1時間以内に収まる最大グループだけとする。同人数の最大グループが複数なら推測せず除外・警告する。保存PT6人・公式partyCount 6・履歴4人の場合は、未参加2人を獲得0として含め6人分配する。異なる難易度候補のユーザー選択・合算は維持する | 同一開催回だけでは、保存メンバーが別PTで倒したLucid等をWill用PTの分配候補へ誤混入するため。秒単位の一致では実データの数分差を拒否する一方、1時間幅と公式PT人数の併用なら6人討伐・4人参加を維持しつつ別PT混入を抑制できる。LULU-081の`partyCount`・`clearedAt`を除外条件にしない部分を置き換える |
| LULU-083 | 2026-08-02 | ✅ | **報酬metadata取得を全履歴取得後の一括処理から、各メンバー履歴直後の逐次取得へ変更する**。同一job内ではitemIdごとに1回だけ取得し、成功metadataは即時に長TTLキャッシュする。hard timeout到達後の未取得metadataは引き続き`metadata_timeout`としてfail-visibleにする | 6人分の上流履歴取得が45秒近くかかるとmetadataを1件も取得できず、キャッシュも作られないため再試行でも同じ失敗が続いた。先行履歴のmetadataを早期取得すれば、後続上流遅延があっても取得済み報酬の分類を保持できる |
| LULU-084 | 2026-08-02 | ✅ | **公式item metadataをVPS側SQLiteへ永続キャッシュする**。取得順はメモリ→SQLite→MSU API、成功した正規化済み公開フィールド（itemId・名称・カテゴリ・tier0・tier1・公式画像URL）だけを30日・最大10,000件保存する。期限切れは再取得し、失敗・UNKNOWN・APIキー・wallet・assetKey・ラッフル履歴は保存しない。systemdの専用StateDirectoryを0700、DBを0600相当で運用する | metadataはユーザー固有情報ではなく再利用性が高い一方、プロセス再起動でメモリキャッシュが失われると上流呼び出しとmetadata_timeoutが再発するため。GitHub PagesやWeb bundleへ含めずVPS内に限定してレート制限と初回待ち時間を削減する |
| LULU-085 | 2026-08-02 | ✅ | **Power Crystalは公平な分配価値へ換算するが、交換・送付できないため実送金原資には含めない**。送金可能額はボスNESO・Ascendant NESO・コイン売却・装備売却だけとする。PT設定で持ち越しを任意に有効化し、前回持ち越しは`+`=受取、`-`=支払、PT内合計0を必須とする。各支払者の実支払を本人の送金可能NESO以下に制限し、未払い義務は負、未受取権利は正で次回へ持ち越す。コインは数量、装備は公式アイコンと名前ツールチップを分配表へ表示する | Power Crystalそのものをメンバー間で移せず、換算差額をNESOだけで当週に全額調整できない実運用と、既存スプレッドシートの繰越運用を正確に再現するため。ユーザー指定 |
| LULU-086 | 2026-08-02 | ✅ | **Party Clearsの人数モデルを「公式討伐人数・保存PT内の履歴参加人数・保存PTの分配人数」の3値へ一般化する**。各値は1〜6人で独立し、6-4-6、6-5-5その他の組み合わせを固定パターンなしで扱う。同一開催回・同一ボス難易度・同一公式`partyCount`をまとめ、複数履歴は参加時刻1時間以内の一意な最大グループ、履歴1人も候補とする。3値が完全一致しない候補は、討伐N人・履歴H人・分配M人を表示し「登録済みM人で分配する」の明示確認後だけ計算可能とする。API契約は`schemaVersion=3`へ更新する | 公式討伐人数と実際のラッフル参加人数・合意した分配人数は一致しない場合があり、6-4-6や6-5-5だけを個別実装すると別人数で再び停止するため。ユーザー指定 |
| LULU-087 | 2026-08-02 | ✅ | **Raffle Calculator UIポリッシュ(`docs/IMPL_PLAN_RAFFLE_UI_POLISH.md`)実装+統括実機検収=合格(コミット・push未実施)**。ユーザー報告「UIが最悪・見たい情報が見にくい」を受け、統括が fixture モード(port 8783 / web 5174 `--mode review`)で実測し確定した問題を実装担当が是正: ①**タイポグラフィ**=精算テーブル本文12.2→13.9px・バッジ10.5→12.2px・メトリクス15.6→17.9px、**ページ全体で<11pxの利用者向けテキスト0件**(実測最小11.56px) ②**「実際の送金」をテーブル→強調リスト化**(`pachimi → 0kn0 525,000,374 NESO`、金額17px/900) ③**ステップ導線**=見出しに1./2./3.、取得状況カードを「2.履歴の読み込み」へ統合、完了/計算後にscrollIntoView(reduced-motion対応。**ペイン環境はフレーム合成停止でsmoothが進まないため実ブラウザでの目視はユーザーローカル確認に委譲**) ④**生技術文字列の全廃**=進捗を人間語+プログレスバー(ms表示廃止)、開催回をローカル+UTC併記(`2026/7/30(木) 9:00（あなたの時刻）/ 00:00 UTC`)、警告・エラーをコード→ローカライズ文言+次の行動へ変換(未知コードは汎用文+`(code)`、精算エラーはメンバー名・ドロップ名へ解決=`pachimi — Phantasma Coin の売却総額（NESO）の値が大きすぎます。`を実機確認) ⑤**操作性**=PC換算レートをインライン`1 Power Crystal = [入力] NESO`化、削除ボタン枠付き化、チェックボックス1.05rem、タブに日本語補助ラベル。**検証**: 384テスト全緑(358既存+26新規)・build成功・6ロケール141キー完全パリティ・en切替で生キー露出なし・light/deep両テーマ視認性確認・計算結果数値は変更前後で完全一致(挙動不変)・差分9ファイル+新規`uiText.js`/`.test.js`(domain/server不変・`git diff -w`ノイズなし・新規npm依存0)。レビュー用に`.claude/launch.json`へ`web-review`(port 5174)と`exp_ranking/web/.env.review.local`を追加(`--mode review`時のみ有効)。**最終の見た目・スマホ・6言語の目視確認はユーザーのローカル検証(PR-012)。コミット・push・VPS配備はユーザー指示待ち** | ユーザー指示(デザイン修正)+統括実機検収(実装担当2ラウンド) |
| LULU-088 | 2026-08-02 | ✅ | **精算表示の追修正(ユーザーローカル確認フィードバック起点)実装+統括実機検収=合格(コミット未実施)**。指摘①「カテゴリ別合計の空欄が広すぎ」→ 表ラッパーに `raffle-table-scroll--compact`(max-width 46rem)で左寄せ化(実測 782px / カード幅 1420px)。指摘②「メンバー別が見にくい」→ (a)ゼロ値は減光した`0`のみ(サブ行なし、実測 #94a3b8 / deep #64748b。**「—」にせず0表記維持=空欄と0の区別原則**) (b)「交換・送金不可」の毎行反復を廃止し PC 列ヘッダーに1回(既存キー再利用=i18n 変更なし) (c)PC換算サブ行はレート≠1のときのみ (d)「当選総額」以降に border-left 区切り(`raffle-col-settle`、deep 対応) (e)ゼブラ+ホバー(sticky 先頭列も同背景で縞崩れなし) (f)member table min-width 68→56rem。変更2ファイル(SettlementResult.jsx / raffle.css)。**384テスト緑・build成功・<11px 0件維持・light/deep 両実測・計算数値不変**。ユーザー最終目視待ち | ユーザーフィードバック+統括実機検収(実装担当第3ラウンド) |
| LULU-089 | 2026-08-02 | ✅ | **精算結果を「ぱっと見サマリー→内訳」の2層構成へ再構成(ユーザーがMapleHubスクショを参照例として指定)+統括実機検収=合格(コミット未実施)**。新構成: ①ヒーロー指標2枚(合計価値・1人あたり分配、値1.6rem=実測27.2px・emerald地) ②サブ指標チップ(送金可能NESO・分配人数・余り) ③メンバー別サマリーカード(名前+当選総額+拡大 支払う/受け取る/精算不要バッジ、±符号だけの表現は不採用=文言優先) ④実際の送金リスト ⑤「内訳」見出し下に既存のカテゴリ別合計(compact幅)+メンバー別テーブル(ゼブラ・区切り・ゼロ減光維持)。DOM順序を実機で確認(hero→cards→transfers→breakdown)。計算ロジック不変(表示のみ・バッジ判定はテーブルとカードで単一ヘルパー共有=重複実装なし)。新規i18nキー1(`raffle.breakdownHeading`)×6ロケール=142キー完全パリティ。**384テスト緑・build成功・<11px 0件維持(実測最小11.56px)・light/deep両対応**。変更: SettlementResult.jsx / raffle.css / 6ロケール。ユーザー最終目視待ち | ユーザーフィードバック(MapleHub参照)+統括実機検収(実装担当第4ラウンド) |
| LULU-090 | 2026-08-06 | ✅ | **コイン・装備の価格入力は販売手数料控除前の売却価格とし、5%控除後の受取額を入力欄直下へ即時表示する**。分配価値・送金可能NESOには loor(売却価格 × 95 / 100) の控除後受取額だけを使用し、空欄・不正値は従来どおり計算を止める。 | 利用者がマーケット上の売却価格をそのまま入力でき、手数料控除の手計算と入力ミスをなくすため。ユーザー指定 |
| LULU-091 | 2026-08-13 | ✅ | **マージ準備(`docs/IMPL_PLAN_RAFFLE_MERGE_PREP.md`)実装+統括検収=合格。push・PR・マージは未実施(ユーザー専権)**。ユーザーのNO-GO判定の各ブロッカーを解消: ①未コミット差分を3コミットへ整理(`5b16b27` 5%手数料 / `e6c21a6` UIポリッシュ / `8f94f93` docs+再採番。`.env.review.local`・`IMPL_PLAN_ranking-cap.md` は非コミット維持) ②raffle決定24件を LULU-067〜090 へ再採番(節見出しも main と衝突しない番号へ。本文相互参照追随・全ファイルでID重複0を実測) ③`origin/main`(109コミット)をマージ `c099bd2`(競合4ファイル=App.jsx/useHashRoute.js/BoardHeader.jsx/vitest.config.js を「mainを正+raffle要素の再結線」で解消) ④T7スパイク残骸8ファイル除去 `8ddb2aa`(`.gitignore` のraffle正当分は維持)。**統括の独立検証**: web **566テスト全緑**(main側+181含む)・build成功・raffle-api 48緑・bot 175緑・`git diff origin/main...HEAD` に t7/env.review/ranking-cap **0件**・fixture実機で2人フロー再確認(合計950,000,750=コイン10億の95%+600+100+50、1人あたり475,000,375、送金1件、**5%手数料が入力直下・カテゴリ表・ヒーローに一貫反映=LULU-090準拠**)。**ブランチは元々未push(履歴書き換え・force push不要)**。watch: ナビ順は現在 EXP Ranking / Task Manager / Enhance History / Raffle Calculator(LULU-068の「Task Managerの隣」はEnhance History追加前の決定のため要ユーザー裁定) | ユーザーNO-GO判定の解消指示+統括検収(実装担当) |
| LULU-092 | 2026-08-13 | ✅ | **sandbox APIキーはローテーションせずそのまま本番運用に使用する(ユーザー裁定「きーはそのままで良い」)**。LULU-069 の「会話内提示済みキーは本番相当運用前にローテーション必須」条項を部分置換。その他のキー保護条項(VPS環境変数のみ・ログ不出力・クライアント不配布)は全て有効のまま | ユーザー裁定(2026-08-13)。露出先が本人のAI作業セッションに限られリスク許容と判断 |
| LULU-093 | 2026-08-13 | ✅ | **ナビ順は現状維持(案A)**: EXP Ranking / Task Manager / Enhance History / Raffle Calculator。LULU-068 の「Task Manager の隣」は Enhance History 追加前の記述として本entryで更新(新ツールは末尾に追加する規則)。LULU-091 の watch-item クローズ | ユーザー裁定(2026-08-13) |
| LULU-094 | 2026-08-13 | ✅ | **raffle-api を新VPS(163.44.118.206)へ本番配備完了(統括が実施・ユーザー委任+ssh/scp許可)**。**重要な逸脱2点**: ①deploy example の port **8782 は新VPSで `live-exp.service` が使用中**(8785 も sf-history が使用)のため **8786** で配備 ②example の `ProtectHome=true` は `/home/botuser/apps` 配下の実行を遮断し **203/EXEC で起動不能**→ `ProtectHome=read-only` へ修正(他の堅牢化・StateDirectory・MemoryMax=192M は維持)。リポジトリの example/README も 8786+read-only へ追随修正。**構成**: `/home/botuser/apps/lulumi-tools-raffle-api`(botuser・venv)+ `/etc/lulumi-tools/raffle-api.env`(root 0600、キー値は転送のみで会話・ログ非経由、`RAFFLE_API_ALLOWED_ORIGINS=https://lulumi-tools.com` 追記)+ systemd `raffle-api.service`(enabled)+ Caddy `(raffle_upstream)` snippet+`handle /raffle/*`(バックアップ `Caddyfile.bak-20260813-raffle`・validate 通過後 reload)。**本番検証全合格**: `/raffle/v1/health` 200(fixtureMode:false・persistentItemCache:true)/ 既存経路無傷(`/healthz` 200・`/sf-history/*` FastAPI応答)/ CORS preflight 200・allow-origin=`https://lulumi-tools.com` / **実キャラ検索E2E成功**(pachimi Lv.254 を実キーで解決)/ 8786 は localhost バインドのみ / MemAvailable 503MB。ロールバック: Caddy handle 削除+reload、`systemctl disable --now raffle-api`(静的サイト・img-proxy 非影響=LULU-040 不変条件維持) | ユーザー委任(ssh/scp 許可 2026-08-13)+統括実施・検証 |
| LULU-095 | 2026-08-13 | 🔶 | **別PC検証用の一時チェックサイトを公開(検証完了後に撤去予定=🔶)**。URL=`http://163.44.118.206:8783/raffle-check/`。構成: `/raffle-check/` ベースで再ビルドした dist(2026-08-12 v2データ同期済・本番API向き)を **Task Manager 静的配信ルート配下**(`lt-taskmanager/current/raffle-check/`)へ配置=**Caddyfile 無変更**。CORS は秘密ファイル不触の **systemd drop-in + `/etc/lulumi-tools/raffle-api-origins.env`**(`EnvironmentFile` 後勝ち)で `http://163.44.118.206:8783` を追加(設定配置・再起動はユーザー実行、E2E=公開URLから実キャラ検索成功を統括確認)。当初案の gear.lulumi-tools.com 転用は**権限クラシファイアが Caddyfile 変更を一貫ブロックしたため中止**(Caddyfile は無変更・gear 影響なし。`sf` は SH-24 本番のため最初から対象外)。**撤去リスト(マージ後)**: ①`raffle-check/` ディレクトリ ②origins drop-in 2ファイル+`daemon-reload`+api再起動 ③残骸 `/var/www/raffle-check` と `/tmp` の作業ファイル ④不要バックアップ `Caddyfile.bak-20260813-raffle2`。既知の制約: http配信のためクリップボードAPI無効・共有画像はCORS外 | ユーザー要望(出先から別PC検証)+統括実施(ユーザー1コマンド協力) |
| LULU-096 | 2026-08-13 | ✅ | **同一ボス・同一難易度で公式partyCountが異なる複数クリアを、それぞれ独立候補として返し、Web側の複数選択で合算・分配できるようにする(LULU-079/083の「履歴人数最大の1クラスタのみ採用」を置換)**。実ケース: 保存PT6人でA〜Eが5人討伐Hard Will、Fが別PTの6人討伐Hard Will→現行はFのクラスタが黙って破棄され5人分しか読めない。修正後はボス×難易度ごとに成立全クラスタ(observed partyCountごとの1時間窓一意最大グループ)を候補化(clearId=`clear-{boss}-{difficulty}-p{partyCount}`で一意化)、ambiguous判定はpartyCount単位で当該クラスタのみ警告・除外。人数不一致の明示確認は選択した各source clearごとに従来どおり要求。Lucid/Will分離・1時間窓・対象ボス限定は不変。詳細 `docs/IMPL_PLAN_RAFFLE_MULTI_CLUSTER.md` | ユーザー指定(2026-08-13 実利用で検出)+統括の根因特定(normalizer.py の largest_clusters 単一選択) |
| LULU-097 | 2026-08-13 | ✅ | **精算表示の価値/実NESO明確化=案A採用(表示のみ・計算不変)+ 精算結果の1ボタン画像コピー機能を追加**。①PC換算レート>1で「1人あたり基準額」が実受取NESOより大きく見える誤解への対処: ラベルを「1人あたりの分配価値（PC換算込み）」へ変更+注記、メンバーカードに「うちPC換算 X NESO（送金対象外）」内訳、サブ指標に「実際に動くNESO合計」(送金合計)。案B(実NESO表示モード)は精算結果の二重化リスクで不採用、案C(PC除外運用)は要望不適合で不採用。②T7方式のcanvas直描画で精算サマリー(ボス・開催回・合計・メンバー別・送金一覧)をPNG生成しクリップボードコピー(非secure contextはダウンロードフォールバック)。**現在の表示ロケールで描画**(T7の英語固定はSNS向け・本件はPT内共有のため目的が異なる)。X共有なし・外部画像なし・新規依存なし。数値は画面と同一ソースを最重要要件とする。詳細 `docs/IMPL_PLAN_RAFFLE_VALUE_CLARITY_SHARE.md` | ユーザー裁定(案A・スクショ共有要望 2026-08-13)+統括設計 |
| LULU-098 | 2026-08-13 | ✅ | **LULU-096(複数クラスタ)+LULU-097(案A+画像コピー)の実装検収=合格・チェックサイト反映済み**。①複数クラスタ: `3b38450`(normalizer=全クラスタemit・clearId `-p{N}` 一意化・ambiguousはpartyCount単位)+`e002699`(**実装担当が契約検証の欠陥を発見**=同一難易度2件目を `invalidClear` で拒否→キーに partyCount 追加・上限6→36)。ユーザー実ケース(5人討伐A〜E+6人討伐F→2候補→合算→6人分配)をサーバー・Web両テストで数値固定。**VPS反映済み(ユーザー再起動14:33実施を統括確認)**。②案A+画像: `cb9f7ec`(ラベル「1人あたりの分配価値（PC換算込み）」+注記/「うちPC換算 X NESO（送金対象外）」/「実際に動くNESO合計」)+`68dbb62`(canvas直描画PNG・現ロケール描画・クリップボード→失敗時自動DL。**T7とはクリップボード/DL処理のみ共通ヘルパー `shareImageIO.js` へ挙動保存抽出**=描画方式はT7がhtml-to-image(キャラ画像)・raffleがcanvas直描画で意図的に別)。**統括実機検証**: fixture(レート1.2)でPC 100→120 NESO換算が内訳・合計に一貫/画像ボタン=blob生成→clipboard試行→フォールバックDL(`lulumi-tools_raffle-settlement_2026-08-13.png`)+ローカライズ済みステータス文言を確認。**web 589テスト(54ファイル)全緑・raffle-api 50緑・build成功・6ロケールパリティ**。watch: T7共有(ShareImageButton再配線)はテスト緑+挙動保存抽出だが、ユーザーローカル確認時にマイキャラ共有も一度試すこと推奨 | 統括検収(実機・fixture)+実装担当2件 |
| LULU-099 | 2026-08-13 | ✅ | **Power Crystal レートを「1 NESO = X PC」の除算方式へ変更(LULU-097①の乗算表記と、LULU-085相当の「割り切れなければ丸めずエラー」のPC経路を置換)**。背景: ゲーム内変換が「1 NESO→1.1 PC」形式で、ユーザー間の「パワクリ1.1倍」表現と入力値を一致させるため(直前のLULU-098検証で乗算1.1がPCを約21%過大評価する逆向き設定になる問題を確認済み)。仕様: 換算=PC数÷レート(メンバーごと)、**丸め=四捨五入**(BigInt厳密有理数・float禁止。100M÷1.1=90,909,091 / ÷1.2=83,333,333 / ÷1.3=76,923,077 をテスト固定)、レートは>0必須(0・空欄はinvalid_rate。PC除外は既存チェックで)、既定値1.1、UIは`1 NESO = [1.1] Power Crystal`+補足「設定したレートで割って計算します」。丸め廃止判断の根拠=除算では割り切れないのが常態で旧エラー方式と両立不能、誤差は±0.5 NESO/人で無視可能、方向はユーザー例示に一致。詳細 `docs/IMPL_PLAN_RAFFLE_PC_RATE_DIVIDE.md` | ユーザー詳細指示(2026-08-13)+統括の丸め方式確定 |
| LULU-100 | 2026-08-13 | ✅ | **LULU-099(PCレート除算方式)実装検収=合格・チェックサイト反映済み**。コミット `1833ab3`(domain: `divideRoundHalfUp` によるBigInt厳密除算・四捨五入、PC経路の `fractional_neso` 廃止、rate=0/0.0→`invalid_rate`)+`2eec869`(UI・6ロケール: `1 NESO = [1.1] Power Crystal`・既定1.1・補足文言・`PC ÷ {rate}` 表記)。**受け入れ数値全固定**: 100M PC÷1.1=90,909,091 / ÷1.2=83,333,333 / ÷1.3=76,923,077 / ÷1.0=100M、分配権利・支払受取・繰越・共有画像モデルまで単一換算値から一貫(二重変換0)。全数確認: **レートは非永続(stateのみ)=移行不要を実測確認**、旧表記grep 0件、carryover一貫。**統括実機(fixture)**: 既定1.1・新ラベル・補足文・`100 PC ÷ 1.1 → 91 NESO` が内訳・合計(950,000,741=コイン95%+600+91+50)まで一貫を確認。**web 594テスト全緑・build成功・6ロケールパリティ**。サーバー不変=VPS再起動不要。watch: `fractional_neso` のUI側マッピング・文言は到達不能コードとして残置(別PRクリーンアップ候補) | 統括検収(実機・fixture)+実装担当 |
| LULU-101 | 2026-08-13 | ✅ | **Raffle Calculator リリース承認(ユーザー「一旦これで完成とし、公開します」)= push・PR・`[web-only]` マージ・本番確認・一時検証環境撤去(LULU-095)まで一括実行を統括に委任**。push 直前状態: origin/main に対し behind 0 / ahead 21、web 594テスト全緑・raffle-api 50緑・build成功、tracked クリーン(未追跡は非コミット方針の5ファイルのみ)。本番 raffle-api は配備済み(LULU-094)・health 200。公開後の残クリーンアップ: 検証サイト撤去・CORS一時オリジン除去(要ユーザー1コマンド)・`fractional_neso` UI残置コードの別PR整理 | ユーザー承認(2026-08-13)+統括最終検査 |
| LULU-102 | 2026-08-13 | ✅ | **Raffle Calculator 本番公開完了+一時検証環境の撤去完了(LULU-095クローズ)**。①push→**PR #20**→`[web-only]` マージ `bf26c8b`(2026-08-13 07:37 UTC)→Pages run 31678554515 = build/commit-db/deploy **全success**。②**本番実機検証合格**: `lulumi-tools.com/#/raffle` でナビ4項目・開催回表示(2026/8/13木 00:00 UTC回)・**本番オリジンからの実キャラ検索成功**(pachimi Lv.254、api.lulumi-tools.com 経由)。③**撤去完了**: 検証サイト `raffle-check/`(TM配下)と `/var/www/raffle-check` 削除(現404確認)・CORS一時オリジン(drop-in+origins.env)除去→raffle-api 再起動 active・**ACAO が `https://lulumi-tools.com` のみへ復帰し 8783 オリジンは拒否されることを実測**・ローカル一時 tgz 削除。残置(意図的): `.claude/launch.json` の web-review 設定と `.env.review.local`(再検証用・非コミット)・`Caddyfile.bak-*`(履歴として無害)。バックログ: `fractional_neso` UI残置コードの整理(LULU-100 watch)・raffle-api の运用監視はT7同様ヘルスチェックのみ | 統括実施(ユーザー公開承認 LULU-101 に基づく)+本番実機検証 |
| LULU-103 | 2026-08-13 | ✅ | **送金通知コピー機能を追加(LULU-069 の「wallet を Web へ返さない」を一部改定)**。「実際の送金」から`送り主 → 受取人 金額 NESO`+受取人ウォレットの2行形式(件間空行)の通知テキストを1ボタンでコピー。表記は**キャラ名**(Discordメンション不要=ユーザー裁定)。**受取ウォレットはAPIから自動取得**(ユーザー裁定=手入力の打ち間違いによる誤送金リスク排除を優先): raffle-api がジョブ応答に**リクエストPTメンバー自身の owner wallet のみ**を返す(`0x`+40hex形式検証・ログ不出力とキャッシュ非保存は不変)。ウォレット未解決メンバーは黙って欠落させず明示表示。コピーは clipboard→textarea フォールバック(http環境対応)。詳細 `docs/IMPL_PLAN_RAFFLE_TRANSFER_OPS.md` C2 | ユーザー要望+裁定(2026-08-13) |
| LULU-104 | 2026-08-13 | ✅ | **PTメンバー順をキャラ名で正規化してから分配計算する(入力順依存の廃止)**。背景: 端数+1配分先と送金ペアリングが入力順依存で、入力し直し・人による順序差で「誰が誰に送る」が変わる非決定性があった(ユーザー指摘)。仕様: **casefold したキャラ名のコードポイント降順・同名は assetKey 昇順**(localeCompare 禁止=端末非依存)、表示順=リクエスト構築順=計算順を一致させる(保存形式は不変・表示計算時に正規化)。文言「余り（PT順に+1）」→「余り（キャラ名順に+1）」。注意: 改名で並びは変わり得るが識別は assetKey のため金額は不変。詳細 同計画 C1 | ユーザー提案+統括の比較規則確定(2026-08-13) |
| LULU-105 | 2026-08-13 | ✅ | **公開後第2ラウンド(ブランチ `raffle/share-image-polish`)実装検収=合格・push待ち(ユーザー「全部完成した後にpush」)**。内容: ①共有画像の枠+縞+精算ピル化と注記文言削除(`717152c`) ②メンバー順のキャラ名降順正規化(`924aa4c`、LULU-104。異順入力→同一 settlement 出力をテスト固定) ③送金通知コピー(`0b356ff`、LULU-103。サーバーが `memberWallets` を応答に追加=LULU-069 一部改定・**wallet のログ不出力/キャッシュ非保存は維持を実測**、`0x`+40hex 検証、clipboard→textarea フォールバック) ④送金行レイアウト=左詰め`送り主 → 受取人 金額`+右端に受取ウォレット全文(`1545924`、C3。画面と共有画像の両方。**wallet の画像掲載はユーザー明示指示=旧「コピー結果へ wallet を含めない」を置換**)。**統括実機検収**: 新レイアウト PNG を実レンダリングしユーザー確認用に提示、fixture E2E で名前降順・送金行(名前+金額+ウォレット一致)・通知コピー出力(`pachimi → 0kn0 498,750,370 NESO\n0x...`)を確認。**web 620テスト・raffle-api 50 全緑・build成功・6ロケールパリティ**。反映時はサーバー変更のため VPS raffle-api 更新+再起動が必要 | 統括検収(実機・fixture)+実装担当(3コミット) |
| LULU-106 | 2026-08-13 | ✅ | **第2ラウンド追補(C4/C5、ユーザー画面フィードバック起点)実装検収=合格(`5124516`)**。①**C4**: 共有画像のメンバー欄を、画面の「メンバー別当選・精算」表と同構成のテーブルへ(**ラッフル履歴・分配権利額の2列は除外=ユーザー指定**。PC=個数+換算NESO 2段・コイン=×個数+売却NESO 2段・装備はアイコン不可のため金額/—・ゼロ減光・include連動)。列集合とセル整形は新ヘルパー `settlementCategoryColumns`/`settlementMemberCategoryCell` に**一本化し画面と画像で共有**(二重実装なし) ②**「実際に動くNESO合計」を廃止**(画面・画像・キー削除6ロケール) ③**C5**: 「実際の送金」を画面・画像とも**3列テーブル**(支払う人→受け取る人/送金額=右寄せ縦ぞろえ/受取ウォレット)へ。画像は全行の金額x座標固定をテストで検証。**統括検収**: web 630テスト全緑・build成功・実レンダリングPNGで全要素確認しユーザーへ提示。push は引き続きユーザー承認待ち | ユーザー指示(2026-08-13)+統括検収(実機) |
| LULU-107 | 2026-08-13 | ✅ | **第2ラウンド(LULU-105/106=画像デザイン・順序正規化・送金通知コピー・送金/メンバーのテーブル化)リリース承認(ユーザー「これで完成とします」)**。手順: VPS raffle-api 先行更新(app.py/job_queue.py/normalizer.py 差し替え+再起動=memberWallets 対応を web 公開前に有効化)→ push → PR → `[web-only]` マージ → 本番検証。push 直前状態: behind 0 / ahead 8・web 630 / raffle-api 50 全緑 | ユーザー承認(2026-08-13) |
| LULU-108 | 2026-08-13 | ✅ | **第2ラウンド本番公開完了**。①VPS raffle-api 先行更新(app.py/job_queue.py/normalizer.py・再起動 active・health 200)②**PR #21** → `[web-only]` マージ `a992f0e` → Pages run 31696151563 = build/commit-db/deploy **全success** ③**本番実弾検証合格**: 実キャラ(pachimi)の本番ジョブで `memberWallets` に有効な `0x`+40hex ウォレットが返却(status complete)/新バンドル `index-BhHPrzkz.js` に新機能文言を確認。今回は一時検証環境を作らなかったため撤去作業なし。残バックログ変わらず(`fractional_neso` UI残置整理) | 統括実施(LULU-107承認に基づく)+本番検証 |
| LULU-110 | 2026-08-13 | ✅ | **ラッフルページのテーマをEXPランキングから分離(ブランチ `raffle/theme-scope`・`b00daf6`)実装検収=合格**(採番注: 当初 LULU-109 で起票したが、並行作業が main へ同IDの cron観測エントリを先着させたため LULU-110 へ再採番)。新保存キー `maplen-board-raffle-theme-v1`(LULU-008 準拠)。load/normalize/save はキー引数の共通モジュール `siteTheme.js` へ一般化(ranking/raffle で共有・二重実装なし・13テスト)。**初回のみ ranking テーマをシード**(分離瞬間の見た目不変)→以後完全独立(再シードなしをテスト固定)。dataset 書き込みの単一書き手構造・TM/starforce の dashboardStore テーマは不変。**統括実機検証**: raffle で depth 変更→raffle キーのみ更新・ranking 不変・ページ往復で各自のテーマ維持。**web 643テスト全緑・build成功** | ユーザー指示+統括検収(実機) |
| LULU-111 | 2026-08-13 | ✅ | **LULU-110(ラッフルテーマ分離)本番公開完了**。PR #25 → `[web-only]` マージ `7aaf753` → Pages deploy 全success → 本番バンドル `index-CDc8e1n3.js` に `maplen-board-raffle-theme-v1` を確認。運用メモ: 並行作業レーン(T12 P5.5等)と決定IDが再度衝突した(LULU-109)。**今後、複数レーン並行時は起票前に origin/main の最新IDを必ず確認**する | 統括実施(ユーザー承認)+本番検証 |
| LULU-115 | 2026-08-14 | ✅ | **キャラ検索をランキング先行(予測変換)+公式API後段の二段構成へ**。①入力途中から**ランキングデータで部分一致の予測候補**を出し、候補クリックで**API呼び出しゼロ**でPT追加(統括が実コードで確認: `BoardProvider` が全ルートで v2 rankings.json を取得済み=raffleページでも**追加通信ゼロ**。8,428件全てが CHAR形式assetKey・worldId・Lv・職を保持し raffle の既存検証に100%適合) ②検索ボタンは従来どおり公式APIの完全一致検索を維持(Lv225未満・ランキング未掲載を拾う)。結果はランキング候補と統合しassetKeyで重複排除、由来をバッジで区別 ③**公式API停止時**は「Lv225以上のランキング掲載キャラは候補から追加可能・Lv225未満はAPI復旧待ち」を明示アナウンスし、**ランキング候補からの追加はブロックしない**。詳細 `docs/IMPL_PLAN_RAFFLE_RANKING_SEARCH.md` | ユーザー指示(2026-08-14)。公式APIメンテ中でもPT作成を可能にし、完全一致入力の負担を下げるため |
| LULU-116 | 2026-08-14 | ✅ | **PTメンバー追加時のワールド一致ブロックを撤去(ユーザー指摘による再調査の結果)**。統括の実コード確認: 当該チェックは**仕様書・DECISION_LOGのいずれにも無い未記録のガード**で、`worldId` は「①当該ブロック ②localStorage保存 ③表示」にしか使われず、**APIリクエスト・討伐マッチング・分配計算のいずれにも不関与**。送金はウォレット間でワールド非依存、別PT討伐の合算・履歴なしメンバーの分配参加は既に許可済み(LULU-078/096)、PT構成はユーザーが明示的に作る前提(LULU-077)であり、ブロックの根拠がない。**当初 LULU-115 の一環で計画していた `worldSource` タグ付け(2つの世界ID語彙の同期面追加)も同時に撤回**——守る価値のないガードのために同期面を増やさない。`worldId` は表示用に保持(読める世界名のときのみ表示・数値IDは非表示)、`raffle.worldMismatch` は6ロケールから削除。誤追加の検出は既存の「ラッフル履歴: なし(当選0)」表示に委ねる(機械的ブロックより情報提示) | ユーザー指摘(2026-08-14「ワールドでブロックする必要ってある？」)+統括の実コード再調査 |
