# SH-15 統括検収(2026-08-05)— **合格**

## (a) パーセンタイル表現

```
現在の位置
この期間の79%より安い          ← 「79パーセンタイル」等の専門用語は画面から消えた
```

方向の確認: 現在値 303.60M / 期間平均 402.28M / 安値 210.56M / 高値 979.79M。
**平均より十分安い状態で「79%より安い」**= 方向が正しい。

## (b) `currentPercentile` は無改変

`git diff 1c5549d..HEAD -- domain/series.js` で計算式(`countAtOrBelow` / `<= currentValue` /
`/ values.length`)に**差分ゼロ**。表示側で `100 − x` にしただけ。

## (c) ★失効計算 — 統括が時計を注入して直接実行

```
MIN=60  MAX=1200
stamp 5分前              →   960.0s  (= 1200 + 60 − 300、式どおり)
stamp 20分前(次が直前)    →    60.0s  ← 下限
★stamp 1時間前           →    60.0s  ← 下限
★stamp 3時間前           →    60.0s  ← 下限
★未来                    →  1200.0s  ← フォールバック
★不正文字列 / None / int  →  1200.0s  ← フォールバック
★誤設定 interval=6000, grace=6000 → 1200.0s  ← 上限で頭打ち
全ケースが [60, 1200] 内: True
```

**最も危険な失敗モード(古いスタンプで失効が過去になり毎リクエスト上流を叩く)が
下限で確実に止まっている**ことを確認。

## (f) スタンプ表示

```
現在値
303.60M
2026-08-05 06:20 UTC (水) 時点     ← 20分グリッド上のスタンプ・UTC + 曜日
```

## (g)(h) 既存の性質が維持されている

- 計算条件ブロック: **不在**(SH-13 維持)
- UTC 統一: **維持**(SH-14)
- 4h テーブル: **577,792行 / `707de4ad3f05de93`** = SH-13 検収時と**同一**
- `/prices`: 確定898 / 暫定2 / **間隔 [4.0] のみ**(穴なし)/ `endDate` `provisionalDate` 不変

## 実装担当の判断で評価すべき点

計画書は `SF_HISTORY_LATEST_TTL_SECONDS` を「**上限の意味に読み替えるか廃止**」としていたが、
実装担当は**廃止**を選び、理由を
「**上限1200秒は『必ず守るガード』であり env で押し上げ可能にすべきでない**」と明示した。

**これは正しい。env で無効化できるガードはガードではない。**
統括の実測でも `interval=6000, grace=6000` の誤設定で 1200 に頭打ちになることを確認した。

## 統括が処理した環境の問題(実装の欠陥ではない)

検収開始時、API(8785)が**停止**していた(実装担当のセッション終了とともにプロセスが落ちた)。
`SF_HISTORY_ALLOWED_ORIGINS` 付きで統括が再起動して検収を継続。

> **手順として記録**: 実装担当が起動したサーバーは**そのセッションの終了で落ちる**。
> 検収側は「画面が出ない」を実装の欠陥と決めつける前に、**まず API の生存を確認する**。
> 今回は2回目(SH-7 検収時は CORS 環境変数の欠落、今回はプロセス停止)。
