# SH-13 統括検収(2026-08-05)— **合格**

## (a) 削除5項目 — 画面から消えていることを機械確認

| 対象 | 残存 |
|---|---|
| 「強化費用はこのグループ共通です(代表: ○○)」 | **なし** |
| 「データが欠けている期間は線が途切れます(推測値は表示しません)」 | **なし** |
| ヒートマップの `n=` | **なし** |
| 「計算ポリシー: starcatch-...」 | **なし** |
| 計算条件ブロック(スターキャッチ/チャンスタイム/破壊防止/イベント補正/指標/足/表示時刻/最終更新/取得時刻) | **なし** |

## (b) ★8時間の穴が塞がった

```
endDate         : 2026-08-04T20:00:00Z   (最終確定・不変)
provisionalDate : 2026-08-05T04:00:00Z   (最も新しい暫定点)
確定 898 / 暫定 2

末尾: 16:00(確定) → 20:00(確定) → 00:00(暫定・hourly由来) → 04:00(暫定・latest由来 asOf=05:00)

★全区間の間隔 = [4.0] のみ  ← 系列全体が4時間刻みで穴ゼロ
```

**規約が単純化された**: 4h テーブルに在る = 確定 / 在らず hourly から導けるもの = 暫定。
これで「完成済みだが未保存」と「進行中」が**同じ規則で埋まる**。

## (c) ★4h テーブル不変

`/prices` を5装備分叩いた前後で **577,792行 / ハッシュ `707de4ad3f05de93`** が一致。
**暫定点は保存されていない**(SH-3 の決定性・SH-7 の基準を維持)。

## (d) 統計不変 — 暫定点が2件に増えても

統括が別実行で確認:

```
computeStats 一致 : true
percentile   一致 : true
heatmap      一致 : true (42セル)
暫定点の価格を全て 1 に改竄しても一致 : true
```

**暫定点が1件から2件に増えても除外規約が一般化されている**ことを確認。

## (e) プリセット

```
通常装備      : 0→17 / 0→21 / 0→22
☆20上限の装備 : 0→17 / 0→21[disabled] / 0→22[disabled]  目標☆max=20・値は17に自動クランプ
```

## チャートの見た目

実線1本 + 破線 `5 4` 1本、**中抜きマーカー2個**(= 暫定点2件と一致)。

## 統括の申し送り(実施済み・記録のみ)

計算条件ブロックの削除により、**「この金額はスターキャッチON・破壊防止なしを前提にした期待値」
であることが画面から消えた**。ユーザー裁定として実施したが、将来クレームの元になりうる点を記録する。
戻すなら小さな「?」等の余地はある。**本スライスでは何も足していない。**

## 実装担当の良い判断

`n=` を消したのに「データ件数(n)を示す」と謳っていた**ヒートマップの説明文が過大請求になる**ことに気づき、
6ロケールとも「中央値を示す」に是正した。「何も足さない」の指示は計算条件ブロックの話として厳守しつつ、
**削除によって嘘になった既存文言を直した**のは正しい切り分け。

## 残(block しない)

- `sfh-group-shared-note` の CSS クラスが死んだまま残存(実害なし)
- `pricesState.endDate` / `latestState.latestUpdatedAt` が未使用気味(API 契約上は正当)
