# IMPL_PLAN_SH27 — 期間の既定を 30日にする

前提: SH-26 完了・統括検収済。**ユーザー指示 2026-08-06**:
> 期間のデフォルトは30日にして

## 0. 現状

`SfHistoryRoot.jsx:61` が `useState("150D")`。
`PERIOD_KEYS = ["7D", "30D", "90D", "150D"]`(`domain/series.js:13`)。

## 1. 変更内容

**初期の期間を `"30D"` にする。**

- **定数として名前付きで持つ**(`SH-26` の `DEFAULT_INITIAL_ITEM_ID` と同じ流儀)。
  `"30D"` をコンポーネント内に直書きしない
- **`PERIOD_KEYS` に存在するキーであることを型/テストで担保**する
  (将来キー名を変えたときに、初期値だけ取り残されて壊れないように)

## 2. ★ヒートマップは連動しない(確認事項)

設計 §3-2(SH-11)で **ヒートマップは期間タブに連動しない**(常に全期間 ≈150日)と決めている。
**既定を 30日に変えても、ヒートマップの中身は変わらない**こと。

## 3. スコープ

**変更してよい**:
- `exp_ranking/web/src/sfhistory/domain/series.js`(既定値の定数)
- `exp_ranking/web/src/sfhistory/SfHistoryRoot.jsx`
- 対応する `*.test.js`
- `docs/reports/SH27_DEFAULT_PERIOD.md`

**触らないもの**(1つでも触れたら停止):
- `server/` 配下すべて
- `PERIOD_KEYS` / `PERIOD_DAYS` の**中身**(選択肢は変えない)
- `sliceByPeriod` の**アルゴリズム**
- **ヒートマップの集計**(`weekdayStats.js`)
- SH-7〜SH-26 の性質すべて
- `src/App.jsx` / `src/board/` / `src/pages/` / `src/components/` / `src/taskManager/` / `src/i18n/`
- `package.json` / **VPS** / **元ツリー**

## 4. 受け入れ基準

- **(a)** 初期表示の期間が **30日**。タブの選択状態も 30日
- **(b)** 初期表示の点数が **30日ぶん**(30 × 6 = **180点**。実測値を報告)
- **(c)** 既定値が `PERIOD_KEYS` に含まれることを**テストで固定**
- **(d) ヒートマップ不変**: 既定 150D のときと **セルの中央値・n が1ビットも変わらない**
- **(e)** 期間タブの切替は従来どおり動く(7/30/90/150)
- **(f)** `npm run test` 全緑 / `npm run build` 成功 / **`server/` の差分ゼロ**
- **(g)** SH-7〜SH-26 の性質維持

## 5. 停止条件

1. **(d) が崩れる**(ヒートマップが期間に連動してしまう)
2. §3 の「触らないもの」に触る必要が生じた / 新規依存が必要になった

## 6. コミット

- **ローカルコミット1本**。**`git push` は行わない**(統括がユーザーに確認したうえで行う)。
- **`git add -A` 禁止**。

## 7. 完了報告テンプレ

```
## SH-27 完了報告
- コミット: <hash>
- (a) 初期表示の期間
- (b) 初期表示の点数
- (c) 既定値が PERIOD_KEYS に含まれるテスト
- (d) ★ヒートマップ不変の確認
- (e) 期間タブの切替
- (f) npm test / build / server 差分ゼロ
- (g) SH-7〜SH-26 の性質維持
```
